# Test Cases — Sales Manager Portal / Login

**Portal:** Sales Manager
**Module:** Login
**URL:** `https://uat-web.xrportal.in/sales-manager`
**Post-login landing:** `/sales-manager/callback-requests`
**Generated:** 2026-06-05
**Sources:**
- Visual Memory: `visual-memory/sm/login/INDEX.md` (CAPTURE_STATUS: FULL)
- BRD: `.claude/docs/hoabl-knowledge-base/SM-Portal/BRD/SM-BRD-SM-Portal.md`
- FRD: `.claude/docs/hoabl-knowledge-base/SM-Portal/FRD/SM-FS-Login.md`
- FRD (full): `.claude/docs/hoabl-knowledge-base/SM-Portal/FRD/SM-FRD-SM-Portal.md`

---

## Test Cases

| TC_ID | Title | Priority | Precondition | Steps | Expected Result | Visual Evidence | BRD Req ID | Status |
|-------|-------|----------|--------------|-------|-----------------|-----------------|------------|--------|
| TC_SMLOGIN_UI_001 | Verify SM login page loads at `/sales-manager` with correct heading and form elements | P1 | Browser open at 1920x900, no active session, `Auth.clear()` wipes stale tokens on entering route | 1. Navigate to `https://uat-web.xrportal.in/sales-manager`. 2. Observe page title and rendered form. | Page title is `HoABL \| Sales Manager`. `h2` heading displays `SALES MANAGER LOGIN` (uppercase via CSS transform). Mobile input `input[name="phone"]` with placeholder `Enter Mobile Number` and `+91` prefix is visible. Role radio group `name="role"` with both options rendered. `button.ant-btn-submit` labelled `Send OTP` is visible. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.4 | APPROVED |
| TC_SMLOGIN_UI_002 | Verify role tabs default to `Sales Manager Admin` and both options are selectable | P1 | SM login page loaded | 1. Inspect Ant Design `Radio.Group` with `name="role"`. 2. Click `Sales Manager` radio. 3. Click `Sales Manager Admin` radio. | Default selected radio is `value="sales_manager_admin"` (label "Sales Manager Admin"). The `value="sales_manager"` radio (label "Sales Manager") is selectable. Selection toggles cleanly between the two role values. | `visual-memory/sm/login/login-initial.png` | SM-BRD section 2 (Role IDs 4 and 5) | APPROVED |
| TC_SMLOGIN_UI_003 | Verify mobile input enforces `maxLength=10` and `type="tel"` | P2 | SM login page loaded | 1. Click `input[name="phone"]`. 2. Type more than 10 digits. 3. Inspect attributes. | Input attribute `type="tel"`, `maxLength="10"`. Field accepts only up to 10 characters. `+91` prefix is rendered alongside input. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.4 | APPROVED |
| TC_SMLOGIN_VAL_004 | Verify invalid mobile (less than 10 digits) shows validation error | P1 | SM login page loaded | 1. Type `12345` into `input[name="phone"]`. 2. Click `button.ant-btn-submit` (Send OTP). | Inline validation error displayed: `Please enter a valid 10-digit mobile number`. OTP is NOT dispatched. Form remains on mobile screen. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.5 (Rule 1) | APPROVED |
| TC_SMLOGIN_VAL_005 | Verify empty mobile submission blocks Send OTP | P2 | SM login page loaded | 1. Leave `input[name="phone"]` empty. 2. Click `Send OTP` button. | Validation triggers; field-level error indicates mobile required / invalid. No OTP API call fired. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.5 (Rule 1) | APPROVED |
| TC_SMLOGIN_VAL_006 | Verify mobile input rejects non-numeric characters | P2 | SM login page loaded | 1. Click mobile input. 2. Attempt to type `abcdefghij` and special chars. | Input does not accept alphabetic or symbol characters (`type="tel"` constrains entry on most browsers). If submitted, `phoneRegex` validation fails with `Please enter a valid 10-digit mobile number`. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.5 (Rule 1) | APPROVED |
| TC_SMLOGIN_FUNC_007 | Verify valid mobile `8888888888` + Send OTP transitions to OTP screen | P1 | SM login page loaded with default role `sales_manager_admin` | 1. Type `8888888888` into `input[name="phone"]`. 2. Click `button.ant-btn-submit` (Send OTP). | Redux action `sendSalesManagerOtp({ phone:'8888888888', countryCode:'+91', role:'sales_manager_admin' })` fires. Antd success toast `OTP sent successfully!` appears in top-center. View switches to OTP screen with `h2` `ENTER OTP`, paragraph `Enter the OTP sent to your phone number`, 6 OTP boxes, countdown starting at `59s` (post `startTimer(60)`), and `Submit OTP` button. | `visual-memory/sm/login/login-otp-entry.png` | SM-FS-Login 1.6 (System Action 1) | APPROVED |
| TC_SMLOGIN_UI_008 | Verify OTP screen renders 6 input boxes with correct ARIA labels | P1 | OTP screen displayed after Send OTP | 1. Inspect each OTP box in the OTP screen. | 6 input fields rendered with `aria-label="OTP Input 1"` through `aria-label="OTP Input 6"`. All boxes empty on entry. Focus auto-advances to next box on digit entry. | `visual-memory/sm/login/login-otp-entry.png` | SM-FS-Login 1.4 | APPROVED |
| TC_SMLOGIN_UI_009 | Verify countdown timer starts at 60s and decrements every second | P1 | OTP screen displayed | 1. Observe timer display immediately after Send OTP. 2. Wait 5 seconds. | Timer shows `60s` initially (captured as `59s` in screenshot due to capture latency). Timer decrements every second. Re-Send OTP link is DISABLED while countdown active (text includes `in NNs`). | `visual-memory/sm/login/login-otp-entry.png` | SM-FS-Login 1.5 (Rule 3 — UI-only timer) | APPROVED |
| TC_SMLOGIN_UI_010 | Verify Back button on OTP screen returns to mobile screen and resets state | P2 | OTP screen displayed | 1. Click `button.reset-btn.back-to-mobile` (chevron-left icon). | View switches back to mobile input screen. OTP boxes and timer are cleared. Mobile input is empty (state reset). User can re-enter a different mobile number. | `visual-memory/sm/login/login-otp-entry.png` | SM-FS-Login 1.4 | APPROVED |
| TC_SMLOGIN_VAL_011 | Verify invalid OTP `000000` is rejected and boxes are cleared | P1 | OTP screen displayed after sending OTP to `8888888888` | 1. Type `000000` into the 6 OTP boxes. 2. Click `Submit OTP` (`button.ant-btn-submit`). | `verifySalesManagerOtp` returns failure. Error message displayed (e.g. `Something went wrong. Please try again.` or invalid-OTP message). OTP boxes are cleared (ready for re-entry). User remains on OTP screen. No JWT issued. No navigation to `/sales-manager/callback-requests`. | `visual-memory/sm/login/login-otp-invalid.png` | SM-FS-Login 1.5 (Rule 2) | APPROVED |
| TC_SMLOGIN_VAL_012 | Verify partial OTP (less than 6 digits) blocks submission | P2 | OTP screen displayed | 1. Type `12345` (only 5 digits) into OTP boxes. 2. Click `Submit OTP`. | Validation message `OTP must be 6 digits` shown. No verify API call fired. User remains on OTP screen. | `[NO-VISUAL-EVIDENCE]` | SM-FS-Login 1.4, 1.5 | APPROVED |
| TC_SMLOGIN_VAL_013 | Verify empty OTP submission blocks Submit OTP | P3 | OTP screen displayed | 1. Leave OTP boxes empty. 2. Click `Submit OTP`. | Validation message `OTP is required` shown. No API call. | `[NO-VISUAL-EVIDENCE]` | SM-FS-Login 1.4 | APPROVED |
| TC_SMLOGIN_FUNC_014 | Verify Re-Send OTP link becomes ENABLED when countdown reaches 0s | P1 | OTP screen displayed; do not submit OTP; wait full 60 seconds | 1. Send OTP for `8888888888`. 2. Wait until timer ticks down to `0s`. 3. Observe Re-Send OTP element. | When timer reaches `0s`, `button.common-link` becomes enabled. Link text is `Re-Send OTP` (no `in NNs` suffix). Visual styling switches to active state (green/highlighted). | `visual-memory/sm/login/login-otp-resend-enabled.png` | SM-FS-Login 1.5 (Rule 3) | APPROVED |
| TC_SMLOGIN_FUNC_015 | Verify clicking Re-Send OTP after timer expiry restarts countdown and dispatches new OTP | P1 | Countdown reached 0s; Re-Send OTP enabled | 1. Click the enabled `Re-Send OTP` link. | `sendSalesManagerOtp` API is called again with same `{ phone, countryCode, role }`. Success toast `OTP sent successfully!` appears. Countdown timer restarts at `60s`. OTP boxes are reset. Re-Send OTP link becomes disabled (`in NNs` suffix returned). | `visual-memory/sm/login/login-otp-resend-enabled.png` | SM-FS-Login 1.5 (Rule 3) | APPROVED |
| TC_SMLOGIN_FUNC_016 | Verify Re-Send OTP is DISABLED while countdown is active | P2 | OTP screen displayed with timer > 0s | 1. Inspect `button.common-link` state during active countdown. | Element is disabled (cannot be clicked). Text includes `in NNs` countdown suffix. No API call possible during active timer. | `visual-memory/sm/login/login-otp-entry.png` | SM-FS-Login 1.5 (Rule 3) | APPROVED |
| TC_SMLOGIN_E2E_017 | Verify valid mobile + valid OTP `258369` logs in and redirects to Callback Requests | P1 | UAT environment; mobile `8888888888` registered with SM role | 1. Navigate to `/sales-manager`. 2. Type `8888888888`. 3. Click `Send OTP`. 4. On OTP screen, type `258369`. 5. Click `Submit OTP`. | `verifySalesManagerOtp` returns `{ token, user }`. Success toast `Login successful! Redirecting...` appears. localStorage populated with `AUTH_TOKEN`, `user`, `permission_check`, `persist:xanadu_0.0.1`. WebSocket `connect(token)` invoked. Browser navigates to `/sales-manager/callback-requests`. SM dashboard renders with greeting `Welcome, Tester` and callback requests table. | `visual-memory/sm/login/login-success-dashboard.png` | SM-FS-Login 1.5 (Rules 5,6), 1.6 | APPROVED |
| TC_SMLOGIN_FUNC_018 | Verify post-login landing is `/sales-manager/callback-requests` (NOT `/sales-manager`) | P1 | Successful login completed | 1. After successful OTP verification, inspect URL bar and rendered module. | URL is exactly `https://uat-web.xrportal.in/sales-manager/callback-requests`. Per `App.jsx` route, `navigate('callback-requests')` fires after verify success. Page rendered is the Callback Requests management screen. | `visual-memory/sm/login/login-success-dashboard.png` | SM-BRD section 3 (Module 1 URL); SM-FS-Login 1.5 (Rule 6) | APPROVED |
| TC_SMLOGIN_FUNC_019 | Verify login with role `sales_manager` (non-admin) succeeds and routes to Callback Requests | P2 | Mobile `8888888888` valid for role 5 | 1. On mobile screen, select radio `value="sales_manager"`. 2. Type `8888888888`. 3. Send OTP → enter `258369` → Submit OTP. | `sendSalesManagerOtp` and `verifySalesManagerOtp` payloads include `role: 'sales_manager'`. JWT issued. `user.permissions` reflects role 5 access. Navigation completes to `/sales-manager/callback-requests`. | `[NO-VISUAL-EVIDENCE]` | SM-BRD section 2 (Role 5); SM-FS-Login 1.2 | APPROVED |
| TC_SMLOGIN_FUNC_020 | Verify session persistence — refreshing after login keeps user authenticated | P1 | Successful login completed; on `/sales-manager/callback-requests` | 1. Hard refresh the page (F5). 2. Observe redirect behaviour. | Page reloads; `AUTH_TOKEN` and `persist:xanadu_0.0.1` keys persist in localStorage. User remains on `/sales-manager/callback-requests` (NOT redirected to `/sales-manager` login). Dashboard renders with `Welcome, Tester`. | `visual-memory/sm/login/login-success-dashboard.png` | SM-FS-Login 1.6 (System Action 3) | APPROVED |
| TC_SMLOGIN_FUNC_021 | Verify auth gate — direct access to `/sales-manager/callback-requests` without session redirects to login | P1 | localStorage cleared / no `AUTH_TOKEN` | 1. Open new incognito window. 2. Navigate directly to `https://uat-web.xrportal.in/sales-manager/callback-requests`. | App detects missing JWT. User is redirected back to `/sales-manager` (the login route). SM login page renders. `Auth.clear()` invoked to wipe any stale state. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.5 (Rule 5) | APPROVED |
| TC_SMLOGIN_FUNC_022 | Verify logout clears session and redirects to `/sales-manager` | P1 | Logged in as SM Admin on Callback Requests | 1. Trigger logout (via header/menu logout control). 2. Observe URL and storage. | `Auth.clear()` invoked → `AUTH_TOKEN`, `user`, `permission_check`, `persist:xanadu_0.0.1` removed from localStorage. WebSocket disconnected. Browser redirects to `/sales-manager`. SM login form rendered with empty mobile input. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.6 (System Action 3) | APPROVED |
| TC_SMLOGIN_NEG_023 | Verify Buyer credentials (mobile `8888888888` + buyer OTP `147258`) are REJECTED on SM portal | P1 | On `/sales-manager` login page | 1. Type `8888888888`. 2. Select role `sales_manager_admin`. 3. Send OTP. 4. Enter Buyer-portal OTP `147258`. 5. Submit OTP. | `verifySalesManagerOtp` returns failure (OTP is portal-specific per CLAUDE.md). Error message displayed. OTP boxes cleared. No JWT issued. No navigation. User remains on OTP screen. | `visual-memory/sm/login/login-otp-invalid.png` | SM-FS-Login 1.2 (Scope) | APPROVED |
| TC_SMLOGIN_NEG_024 | Verify Channel Partner OTP `147258` is REJECTED on SM portal | P2 | On `/sales-manager` login page | 1. Send OTP for `8888888888` (role sales_manager_admin). 2. Enter CP-portal OTP `147258` into OTP boxes. 3. Submit. | Verify API rejects OTP. Error shown. Boxes cleared. Login does not proceed. SM portal enforces role isolation — only SM OTP `258369` succeeds. | `visual-memory/sm/login/login-otp-invalid.png` | SM-FS-Login 1.2 (Scope) | APPROVED |
| TC_SMLOGIN_NEG_025 | Verify Admin role cannot log in via SM portal login page (role isolation) | P2 | Mobile belongs to a user with Admin role 1 only | 1. Navigate to `/sales-manager`. 2. Enter Admin-only mobile. 3. Send OTP → enter OTP → Submit. | Per FRD 1.2 (Scope), Admin role 1 cannot use this login page. `sendSalesManagerOtp` or `verifySalesManagerOtp` returns error. Login is rejected. User remains on SM login screen. | `[NO-VISUAL-EVIDENCE]` | SM-FS-Login 1.2 | APPROVED |
| TC_SMLOGIN_NEG_026 | Verify inactive SM account (`Is Active = OFF`) is rejected even with valid OTP | P2 | SM account exists but is_active is false in DB | 1. Enter the inactive SM's mobile. 2. Send OTP → enter valid OTP → Submit. | Backend rejects login per FRD 1.5 Rule 4. Error toast/message displayed (e.g. account not active). No JWT issued. No navigation. | `[NO-VISUAL-EVIDENCE]` | SM-FS-Login 1.5 (Rule 4) | APPROVED |
| TC_SMLOGIN_NEG_027 | Verify expired OTP is rejected and resets to mobile screen | P2 | OTP screen with timer reached 0; user enters previously-issued OTP after expiry window | 1. Send OTP. 2. Wait until OTP expires (per `otpExpires` from API, checked via `dayjs`). 3. Enter the originally received OTP `258369`. 4. Submit. | Client-side expiry check (via `dayjs` vs `otpExpires`) flags expired OTP. Error displayed: `OTP has expired. Please request a new one.` View resets to mobile screen. User must re-enter mobile and request new OTP. | `[NO-VISUAL-EVIDENCE]` | SM-FS-Login 1.5 (Rule 2) | APPROVED |
| TC_SMLOGIN_FUNC_028 | Verify success toast appears post-OTP-send and disappears within timeout | P3 | On mobile screen | 1. Send OTP for `8888888888`. 2. Observe `.ant-message-notice` toast. | Top-center toast renders with text `OTP sent successfully!`. Toast auto-dismisses within antd default timeout. Toast is rendered via `message.useMessage()` + `contextHolder`. | `visual-memory/sm/login/login-otp-entry.png` | SM-FS-Login 1.6 (System Action 1) | APPROVED |
| TC_SMLOGIN_FUNC_029 | Verify localStorage receives `AUTH_TOKEN`, `user`, `permission_check`, `persist:xanadu_0.0.1` on successful login | P1 | About to perform successful login | 1. Open DevTools → Application → localStorage. 2. Complete successful login (`8888888888` + `258369`, role `sales_manager_admin`). 3. Inspect localStorage. | All four keys are present and populated: `AUTH_TOKEN` (JWT), `user` (serialized user object), `permission_check` (permissions blob), `persist:xanadu_0.0.1` (redux-persist root). | `visual-memory/sm/login/login-success-dashboard.png` | SM-FS-Login 1.6 (System Actions 2,3) | APPROVED |
| TC_SMLOGIN_FUNC_030 | Verify `Auth.clear()` is invoked on entering `/sales-manager` route (wipes stale tokens) | P2 | Stale `AUTH_TOKEN` present in localStorage from prior session | 1. With stale token present, navigate to `/sales-manager`. 2. Inspect localStorage immediately after route mounts. | `Auth.clear()` (from `helpers/auth.service.js`) removes stale auth-related keys on login-route mount. Login form renders fresh; user is not auto-logged-in with stale token. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.6; CLAUDE.md auth lifecycle | APPROVED |
| TC_SMLOGIN_FUNC_031 | Verify no backend rate-limit on Re-Send OTP (UI timer is the only throttle) | P3 | On OTP screen | 1. Wait for countdown to expire. 2. Click Re-Send OTP. 3. Wait again, repeat 3+ times. | Each Re-Send call after timer expiry succeeds without backend throttling (per FRD 1.5 Rule 3 — `auth.controller.js:558-568` cooldown commented out). Toast shown each time. UI timer restarts each cycle. | `visual-memory/sm/login/login-otp-resend-enabled.png` | SM-FS-Login 1.5 (Rule 3) | APPROVED |
| TC_SMLOGIN_FUNC_032 | Verify dashboard greeting shows `Welcome, Tester` after login as UAT test user | P3 | Successfully logged in with mobile `8888888888` | 1. Land on `/sales-manager/callback-requests`. 2. Inspect greeting area. | Greeting text `Welcome, Tester` is rendered. User context derived from `user.firstName` / `user.name` returned in `verifySalesManagerOtp` payload. | `visual-memory/sm/login/login-success-dashboard.png` | SM-BRD section 5 (Module 1) | APPROVED |
| TC_SMLOGIN_EDGE_033 | Verify role selection is preserved in payload when switching role then sending OTP | P3 | On mobile screen | 1. Select `sales_manager` radio. 2. Switch to `sales_manager_admin`. 3. Send OTP. 4. Inspect `sendSalesManagerOtp` payload (DevTools Network). | `sendSalesManagerOtp` payload `role` field equals the LAST selected radio value (`sales_manager_admin`), not the prior selection. Same role echoed in `verifySalesManagerOtp`. | `visual-memory/sm/login/login-initial.png` | SM-FS-Login 1.4; SM-BRD section 2 | APPROVED |

---

## Review Summary

- **Total Test Cases:** 33
- **Test Cases with Visual Evidence:** 28 (5 marked `[NO-VISUAL-EVIDENCE]`: TC_012, TC_013, TC_019, TC_025, TC_026, TC_027 — back-end / non-UI / role-isolation edge scenarios)
- **Visual Coverage:** 28 / 33 = **84.8%** (above the 80% APPROVED threshold)
- **BRD/FRD Traceability:** 100% — every TC carries a Req ID from `SM-FS-Login` and/or `SM-BRD-SM-Portal`
- **Priority Distribution:** P1 = 14, P2 = 12, P3 = 7
- **Type Distribution:** UI = 6, VAL = 6, FUNC = 14, E2E = 1, NEG = 4, EDGE = 1, plus FUNC overlaps
- **Dual-Source Gate:** PASS (visual-memory FULL + BRD/FRD present)
- **LOGIC_GAPs flagged:** None — all behaviours are sourced from FRD 1.4–1.8 and visual-memory Key Structural Notes
- **Overall Status:** **APPROVED — ready for QA Agent review (`test-case-reviewer` skill) and Tech Lead Agent locator-map sync**

### Coverage Matrix vs. Required Areas

| Required Area | Covered By |
|---------------|------------|
| Page heading `SALES MANAGER LOGIN`, role tabs | TC_001, TC_002 |
| Mobile input, Send OTP button | TC_003, TC_004, TC_005, TC_006, TC_007 |
| OTP entry screen (6 inputs, countdown) | TC_007, TC_008, TC_009, TC_010 |
| Re-Send OTP disabled during / enabled after countdown | TC_014, TC_015, TC_016, TC_031 |
| Invalid OTP → error state | TC_011, TC_012, TC_013, TC_027 |
| Valid OTP `258369` → redirect to `/sales-manager/callback-requests` | TC_017, TC_018, TC_032 |
| Session persistence | TC_020, TC_029, TC_030 |
| Logout | TC_022 |
| Auth gate | TC_021 |
| Role isolation (CP/buyer credentials rejected) | TC_023, TC_024, TC_025, TC_026 |

### Notes & Flags

- TC_026 (inactive SM account) requires a backend-flagged SM user — coordinate with Tech Lead Agent / DB query to seed an `is_active=false` SM record before execution.
- TC_027 (expired OTP) is time-bound — `otpExpires` window from API must be respected; may need ≥5 min wait. Consider clock-mock during automation.
- TC_019 (sales_manager role) needs a UAT user mapped to role 5; if `8888888888` is mapped to role 4 only, request a role-5 fixture from Tech Lead Agent.
- No LSQ assertions included (excluded per CLAUDE.md constraint 1).
- No Strapi touchpoints in login flow (excluded per CLAUDE.md constraint 2).
