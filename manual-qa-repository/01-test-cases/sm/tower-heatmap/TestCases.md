# Test Cases — Sales Manager Portal / Tower Heatmap

**Module:** Tower and Unit Heatmap (sidebar "Towers")
**Portal:** Sales Manager
**Route:** `https://uat-web.xrportal.in/sales-manager/towers`
**Visual Memory:** `visual-memory/sm/tower-heatmap/INDEX.md` (CAPTURE_STATUS: FULL)
**BRD/FRD Sources:**
- `.claude/docs/hoabl-knowledge-base/SM-Portal/BRD/SM-BRD-SM-Portal.md` (§3 #2, §4.9)
- `.claude/docs/hoabl-knowledge-base/SM-Portal/FRD/SM-FS-Tower-Heatmap.md` (Feature 1, §1.1–1.7)
- `.claude/docs/hoabl-knowledge-base/SM-Portal/FRD/SM-FRD-SM-Portal.md` (§5 Module 2)
**Generated:** 2026-06-05
**Status:** APPROVED — reviewer verdict 2026-06-06 (visual 93.3%, doc-logic 100%, 4 CONDITIONAL TCs flagged for data/env fixtures, no LOGIC_GAP, no VISUAL_MISMATCH). See `review-report.md`.

---

## Source-of-Truth Note on Colour Coding

The legacy FRD §1.5 colour table lists White/Green, Orange, Red, Blue. The **FSD-CORRECTION dated 2026-05-25** in `SM-FS-Tower-Heatmap.md` Step 3 (sourced from `common.service.js` heatmap functions) supersedes it:

| Colour | Statuses |
|--------|----------|
| Green (`#00FF00`) | AVAILABLE, HOLD, PREBOOKED, RESERVED |
| Red (`#FF0000`) | BOOKED, PBT |
| Grey (`#808080`) | REFUGE, NOT_AVAILABLE (synthetic padding) |
| Orange | NOT ACTIVE — code commented out |
| Blue | Does not exist in source |

Visual-memory DOM inspection (2026-06-05) on Tower 9 - Triumph confirms 6 CSS classes mapped to these 3 colour buckets: `available-unit`, `disabled-unit`, `booked-unit`, `refuse-area refuge-unit`, `pbt-unit`, `not-available-unit`. TCs below use the corrected mapping.

---

## Test Cases

| TC_ID | Title | Priority | Precondition | Steps | Expected Result | Visual Evidence | BRD Req ID | Status |
|-------|-------|----------|--------------|-------|-----------------|-----------------|------------|--------|
| TC_SMTWR_UI_001 | Page load — default tower selected | P0 | SM logged in via mobile OTP (`8888888888` / `258369`); session in `automation-repository/fixtures/.auth/sales-manager.json`; at least one active tower configured | 1. Navigate to `https://uat-web.xrportal.in/sales-manager/towers`. 2. Wait for left rail `h6` "Select Tower" and right pane `h5` "Tower View" to render. 3. Observe the default selected tower in the left rail (`<li class="tower-item selected-tower">`). | Page loads with `h5` "Tower View" heading. Left rail shows "Select Tower" list with description "Choose a tower to view available units". A default tower (Tower 8 - Crest in UAT) is pre-selected with `selected-tower` class (green background). Right pane shows `h6.tower-name-title` matching the selected tower (e.g., "Tower: Crest") and renders the unit grid. No errors. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png` | SM-BRD §3 #2; SM-FS-Tower-Heatmap §1.1, §1.3 | APPROVED |
| TC_SMTWR_UI_002 | Tower View page heading and two-column layout | P1 | SM logged in; `/sales-manager/towers` loaded | 1. Inspect page-level `h5` heading. 2. Confirm left rail (Select Tower) and right pane (Heatmap grid) are both visible side-by-side at 1920×900 viewport. | `h5` reads "Tower View". Left rail contains `h6` "Select Tower" + description + scrollable `tower-item` list. Right pane contains tower name title + unit-size header row + floor labels column + heatmap grid + download icon. Layout matches captured screenshot. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png` | SM-FS-Tower-Heatmap §1.4 | APPROVED |
| TC_SMTWR_FUNC_003 | Tower list completeness — all configured towers visible | P0 | SM logged in; project has multiple towers configured | 1. Scroll the left rail tower list. 2. Enumerate all visible `<li class="tower-item">` entries with their `h5` tower name + "X Units Available" text. | Left rail lists all configured towers for the project. UAT visible set includes at minimum: Tower 1 - Dawn, Tower 2 - Aura, Tower 3 - Glory, Tower 4 - Pride, Tower 5 - Grace, Tower 6 - Aspire, Tower 7 - Blossom, Tower 8 - Crest, Tower 9 - Triumph, Tower 10 - Crown, Tower 11 - Prime, Tower 13 - Prestige, Tower 15 - Radiance, Tower 16 - Fortune, Tower 18 - Grand. Each tower-item shows tower name and a units-available count. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png`, `heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.4 | APPROVED |
| TC_SMTWR_FUNC_004 | Tower switching updates grid | P0 | SM logged in; `/sales-manager/towers` loaded with default tower (Tower 8 - Crest) | 1. Click a different tower in the left rail (e.g., "Tower 9 - Triumph") `<li class="tower-item">`. 2. Wait for grid to re-render. 3. Inspect right pane title and grid contents. | `selected-tower` class moves from previous tower to clicked tower (Tower 9). Right pane `h6.tower-name-title` updates to "Tower: Triumph". Heatmap grid re-renders with that tower's units (Tower 9 shows 35 floors × 8 unit columns = ~280 cells, "223 Units Available"). Unit-size header row updates accordingly. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.1, §1.4; SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_UI_005 | Unit-size header row shows per-column configuration | P1 | Tower selected (e.g., Tower 9 - Triumph) | 1. Inspect the top header row of the heatmap grid (`.unit-size-item` cards). 2. Read each card's text. | Each `.unit-size-item` card shows: column label (e.g., "Unit - 1"), area in sq.ft. (e.g., "323"), and typology (e.g., "1 BHK Growth"). 8 unit-columns typical per tower. Header is row-aligned with the grid columns below. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_UI_006 | Floor labels column renders | P1 | Tower selected with multi-floor grid | 1. Inspect the leftmost column of the heatmap grid (`.unit-section-box`). 2. Confirm "Floor" heading and numbered floor labels. | Left column shows `Floor` heading followed by floor numbers ordered top-down (highest floor at top, e.g., 35 → 1 for Tower 9). Floor labels align row-by-row with grid cells. Labels are read-only (not clickable filters). | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_FUNC_007 | Available unit — visual state and click opens detail panel | P0 | Tower 9 - Triumph selected; at least one unit with class `available-unit` present | 1. Hover over an available unit cell `div.unit-number.available-unit` (e.g., 3501). 2. Observe hover styling. 3. Click the cell. 4. Wait for right-side "Unit Details" panel to render. | Hover state applies CSS hover styling on cell border (no separate tooltip element renders — visual hover only, per INDEX.md). On click, right-side "Unit Details" panel mounts adjacent to the grid (NOT a modal — grid remains visible). Panel header `h5` "Unit Details" with green background bar appears. Cells stay on the page. | `visual-memory/sm/tower-heatmap/heatmap-unit-hover.png`, `heatmap-unit-click.png` | SM-FS-Tower-Heatmap §1.4, §1.6 #1; SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_FUNC_008 | Unit Details panel fields | P0 | Available unit clicked (e.g., 3501 in Tower 9 - Triumph) | 1. With Unit Details panel open, read all visible fields. | Panel shows: **Unit No** (e.g., "3501 - Triumph"), **BHK** (e.g., "1 BHK Growth Home"), **Size** (e.g., "323 sq.ft."), **Agreement Value** (₹ value, masked as `₹ xxxxxxxx` in UAT), **Stamp Duty** (₹ value), **GST** (₹ value), **Registration Charges** (₹ value), and **\*All inclusive** info-icon row showing total. No "Allocate" or write-action button is present (read-only). | `visual-memory/sm/tower-heatmap/heatmap-unit-click.png` | SM-FS-Tower-Heatmap §1.4, §1.6 #1; SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_BIZ_009 | Heatmap is read-only — no allocation action from this screen | P0 | Available unit clicked; Unit Details panel open | 1. Inspect Unit Details panel for any "Allocate", "Book", "Hold", or "Reserve" action buttons. 2. Right-click on a unit cell to check context menu. 3. Try double-clicking an available unit. | No write-action controls exist on the panel or the grid. No allocation/booking buttons present. The Tower View is strictly informational. To allocate a unit to a walk-in customer, SM must use the **Physical Allocation** module (`/sales-manager/physical-allocation`), per the explicit Note in SM-FS-Tower-Heatmap "How to Use" Step 4. | `visual-memory/sm/tower-heatmap/heatmap-unit-click.png` | SM-BRD §4 #9; SM-FS-Tower-Heatmap §1.2, §1.6 #1; SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_FUNC_010 | Available unit colour — green status bucket | P0 | Tower 9 - Triumph selected | 1. Identify a cell with class `unit-number available-unit`. 2. Inspect its rendered background/border colour. 3. Cross-reference with FSD-CORRECTION colour map. | Cell renders with white background and green outline (`#00FF00` palette). Maps to status bucket: AVAILABLE / HOLD / PREBOOKED / RESERVED (per FSD-CORRECTION 2026-05-25, `common.service.js`). Tower 9 shows 223 such cells. Cell is clickable and opens Unit Details panel (per TC_SMTWR_FUNC_007). | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.5 (corrected); SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_FUNC_011 | Booked unit colour — red status bucket | P0 | Tower 9 - Triumph selected (has 7 booked-unit cells) | 1. Identify a cell with class `unit-number booked-unit`. 2. Inspect rendered colour. 3. Click the booked cell. | Cell renders with red fill (`#FF0000` palette). Maps to status: BOOKED. Clicking a booked cell does NOT open the Unit Details panel (cell is inert on click per INDEX.md interactions section). | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.5 (corrected) | APPROVED |
| TC_SMTWR_FUNC_012 | PBT unit colour — red status bucket | P1 | Tower 9 - Triumph selected (has 2 pbt-unit cells) | 1. Identify a cell with class `unit-number pbt-unit`. 2. Inspect colour and behaviour on click. | Cell maps to PBT status. Per FSD-CORRECTION, PBT belongs to the red colour bucket (`#FF0000`) — however the UAT live capture shows pbt-unit rendered in cyan/turquoise. **GAP flagged**: visual rendering may not match FSD-CORRECTION colour map for PBT — confirm with developer whether cyan fill is current source-of-truth or stale CSS. Cell is inert on click (no detail panel). | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.5 (corrected) | CONDITIONAL — colour-map gap flagged |
| TC_SMTWR_FUNC_013 | Refuge area unit — grey status bucket | P1 | Tower 9 - Triumph selected (has 6 refuge cells) | 1. Identify a cell with classes `unit-number refuse-area refuge-unit`. 2. Inspect colour and label text. 3. Click the cell. | Cell renders with light grey fill (`#808080` palette) and visible text "REFUGE". Maps to status: REFUGE (per FSD-CORRECTION). Refuge cells represent non-residential floor areas. Clicking does NOT open Unit Details panel — cell is inert. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.5 (corrected) | APPROVED |
| TC_SMTWR_FUNC_014 | Disabled unit — grey status bucket | P1 | Tower 9 - Triumph selected (has 29 disabled-unit cells) | 1. Identify a cell with class `unit-number disabled-unit`. 2. Inspect colour and click behaviour. | Cell renders with grey fill. Maps to a non-selectable status in the current campaign (e.g., NOT_AVAILABLE synthetic padding or admin-disabled). Cell is inert on click — no Unit Details panel opens. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.5 (corrected) | APPROVED |
| TC_SMTWR_FUNC_015 | Not-available unit — grey status bucket | P2 | Tower 9 - Triumph selected (has 13 not-available-unit cells) | 1. Identify a cell with class `unit-number not-available-unit`. 2. Inspect rendering and click behaviour. | Cell renders with grey fill (per FSD-CORRECTION, NOT_AVAILABLE belongs to grey bucket). Cell is inert on click. Status not currently exposed via tooltip or panel — purely visual. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.5 (corrected) | APPROVED |
| TC_SMTWR_UI_016 | Unit count totals — 6 status classes sum to grid total | P1 | Tower 9 - Triumph selected (~280 cells total) | 1. Count cells per CSS class in the rendered grid. 2. Sum and compare to grid dimensions (35 floors × 8 unit columns). | Counts match INDEX.md inspection (Tower 9 - Triumph): 223 available + 29 disabled + 7 booked + 6 refuge + 2 pbt + 13 not-available = 280 cells = 35 × 8. "X Units Available" badge on tower-item in left rail matches the available-unit count (223). | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.4 | APPROVED |
| TC_SMTWR_FUNC_017 | Switching between two towers preserves selected-tower state correctly | P1 | At least 2 towers visible in left rail | 1. Note initial selected tower (Tower 8 - Crest, default). 2. Click "Tower 9 - Triumph" in left rail. 3. Click "Tower 8 - Crest" again. 4. Click "Tower 11 - Prime". | `selected-tower` class is exclusive — only one tower-item has it at any time. After step 2: only Tower 9 selected. After step 3: only Tower 8. After step 4: only Tower 11. Right pane `h6.tower-name-title` updates each time. Grid contents update each time. No stale grid retained from previous tower. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png`, `heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.4 | APPROVED |
| TC_SMTWR_UI_018 | Tower units-available badge in left rail | P1 | Left rail visible with all tower-items | 1. Read the "X Units Available" subtext under each tower's `h5` name in `<li class="tower-item">`. | Each tower-item shows current available count next to/under the tower name (e.g., "Tower 8 - Crest — 84 Units Available", "Tower 9 - Triumph — 223 Units Available"). Counts are tower-specific and reflect available-unit class cell count for that tower. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png`, `heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.4 | APPROVED |
| TC_SMTWR_UI_019 | Preference count badges on unit cells | P2 | Tower selected with at least one unit that has been preference-ranked by customers | 1. Scan grid cells for `<span class="preference-count-badge">` markers. 2. Read badge values. | Small green circular badges appear on units where customers have ranked this unit as a preference. Badge text shows count (values 1–4 observed in UAT). Badges overlay the unit-number cell. Consumed by allocation-experience flows (cross-reference: allocation TCs). | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FRD §5 Module 2 | APPROVED |
| TC_SMTWR_UI_020 | Top banner present on Towers page | P3 | SM logged in; any sidebar page open | 1. Inspect the full-width gray banner at the top of the page above the Tower View heading. | Banner displays: "India's Biggest Growth Housing Revolution Begins On 7th April 2026." Banner is consistent across SM sidebar pages. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png` | SM-FRD §10 | APPROVED |
| TC_SMTWR_FUNC_021 | Download icon visibility on grid | P2 | Tower selected with grid rendered | 1. Locate the download icon at the top-right of the heatmap grid. 2. Confirm presence (do not click — export behaviour out of scope). | A download icon button is rendered at the top-right corner of the heatmap grid area. The control is visible and clickable. (Export behaviour and downloaded artefact validation are not covered by this TC batch — flagged as a separate scope.) | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | INDEX.md observed element — no explicit BRD/FRD requirement | CONDITIONAL — feature observed in UI but not in BRD/FRD; gap flagged for BA confirmation |
| TC_SMTWR_NEG_022 | Booked, refuge, disabled, pbt, not-available cells do not open Unit Details panel | P0 | Tower 9 - Triumph selected | 1. Click in sequence: a `booked-unit` cell, a `refuge-unit` cell, a `disabled-unit` cell, a `pbt-unit` cell, a `not-available-unit` cell. 2. After each click, check if Unit Details panel opens or changes. | None of the five non-available status cells open or update the Unit Details panel. Cells are inert on click (per INDEX.md interactions section). No console errors. Only `available-unit` cells are interactive. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.6 #1 (read-only); INDEX.md | APPROVED |
| TC_SMTWR_NEG_023 | No discrete filter dropdown — tower selection is the sole filter | P1 | Tower View page loaded | 1. Inspect page chrome above and around the grid for any filter control (status dropdown, BHK selector, floor filter). 2. Confirm absence. | No discrete filter UI is present. Filtering of grid contents happens only via tower selection in the left rail. Floor labels are read-only (not clickable filters). `heatmap-filter.png` documents the absence as a fallback page capture. Future BRD changes may add filters — current scope: none. | `visual-memory/sm/tower-heatmap/heatmap-filter.png` | SM-FS-Tower-Heatmap §1.4 (only tower-list + grid + legend listed); INDEX.md | APPROVED |
| TC_SMTWR_BIZ_024 | Inactive towers may be hidden from SM view | P2 | An admin-disabled (inactive) tower exists in the project | 1. Compare the SM tower list in left rail to the admin-side tower list in Admin Portal `/admin/towers`. 2. Confirm any inactive tower is NOT present in SM left rail. | Per SM-FS-Tower-Heatmap §1.6 #2 and SM-FRD §5 Module 2 business rules: "Inactive towers may be hidden from the SM view (admin controls which towers are active)". SM list shows only active towers. (Requires admin-side data setup to validate — flag for data-dependent test.) | `visual-memory/sm/tower-heatmap/heatmap-loaded.png` | SM-FS-Tower-Heatmap §1.6 #2; SM-FRD §5 Module 2 | CONDITIONAL — data-dependent; needs Admin-side inactive tower fixture |
| TC_SMTWR_BIZ_025 | Heatmap reflects live inventory during active campaign (WebSocket) | P1 | A PHYSICAL_EVENT or active allocation campaign is running; SM logged in with `/sales-manager/towers` open | 1. Note the available-unit count for Tower 9 - Triumph. 2. From a separate Admin or another SM session, mark a unit in that tower as BOOKED via Physical Allocation. 3. Wait up to ~5s for WebSocket push. 4. Inspect the original SM session's grid. | Per SM-FS-Tower-Heatmap §1.6 #3: "During an active allocation campaign, the heatmap updates in real-time via WebSocket". The previously-available cell transitions from `available-unit` class to `booked-unit` class without page refresh. Available-unit count in left rail decrements. | `visual-memory/sm/tower-heatmap/heatmap-tower-selected.png` | SM-FS-Tower-Heatmap §1.6 #3; SM-FRD §5 Module 2 | CONDITIONAL — requires active campaign + multi-session setup |
| TC_SMTWR_BIZ_026 | Heatmap reflects last-known DB state when no active campaign | P2 | No active PHYSICAL_EVENT campaign running | 1. Navigate to `/sales-manager/towers`. 2. Inspect tower grid for any tower. 3. Cross-check available/booked counts against DB snapshot. | Per SM-FS-Tower-Heatmap §1.6 #4: "Outside of an active campaign, the view reflects the last known state from the database". Grid loads with last-known unit statuses. No real-time updates expected. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png` | SM-FS-Tower-Heatmap §1.6 #4 | APPROVED |
| TC_SMTWR_NEG_027 | No audit trail expected — no write actions logged | P2 | SM views heatmap, clicks units, switches towers | 1. Browse the heatmap, switch towers, click multiple available units. 2. Check audit/activity logs (admin-side) for any entries from this SM session. | Per SM-FS-Tower-Heatmap §1.7: "No write actions occur from this screen — no audit trail required." No audit log entries are produced from heatmap navigation, tower switches, or unit-detail panel views. | `visual-memory/sm/tower-heatmap/heatmap-unit-click.png` | SM-FS-Tower-Heatmap §1.7 | APPROVED |
| TC_SMTWR_NEG_028 | Unauthenticated access redirects to SM login | P0 | No active SM session (auth state cleared) | 1. Clear cookies and `automation-repository/fixtures/.auth/sales-manager.json`. 2. Navigate directly to `https://uat-web.xrportal.in/sales-manager/towers`. | Page redirects to `/sales-manager` login screen (mobile OTP entry). Heatmap content is not rendered. After successful OTP login, user is returned to Callback Requests landing page (default) — NOT directly back to towers (per SM-FRD §6 navigation default). | (no screenshot — auth gate behaviour) | SM-BRD §2; SM-FRD §7 | APPROVED |
| TC_SMTWR_FUNC_029 | Sidebar navigation to Towers from Callback Requests | P1 | SM logged in on `/sales-manager/callback-requests` (default landing) | 1. Click the "Towers" link/icon in the sidebar or bottom navigation. 2. Confirm route change. | URL changes to `/sales-manager/towers`. Tower View page loads with default tower selected and grid rendered. Sidebar/bottom-nav indicator highlights "Towers" as active tab. | `visual-memory/sm/tower-heatmap/heatmap-loaded.png` | SM-FRD §6 (navigation); SM-FRD §10 (bottom nav) | APPROVED |
| TC_SMTWR_NEG_030 | SM role gate — only roles 5 and 4 can access | P1 | A user with role other than 5 (Sales Manager) or 4 (Sales Manager Admin) attempts to log in to `/sales-manager` | 1. Attempt OTP login at `/sales-manager` with a non-SM mobile number. 2. If login succeeds, attempt to access `/sales-manager/towers`. | Per SM-FRD §7 and SM-BRD §2: only roles 5 and 4 can log into SM portal. Admin (role 1) cannot log in via SM login page (separate auth), though admins CAN access the same routes through their own portal session if applicable. Non-SM users are denied at login. | (no screenshot — auth gate behaviour) | SM-BRD §2; SM-FRD §7 | APPROVED |

---

## GAP & Note Summary (for BA / Tech Lead follow-up)

| Item | TC_ID | Source | Action Needed |
|------|-------|--------|---------------|
| PBT unit cyan/turquoise rendering contradicts FSD-CORRECTION red bucket | TC_SMTWR_FUNC_012 | INDEX.md observation vs SM-FS-Tower-Heatmap §1.5 (corrected) | Confirm whether `pbt-unit` CSS in current build follows the source-of-truth red colour or whether the FSD-CORRECTION is stale. Likely a code-vs-doc drift. |
| Download icon export behaviour | TC_SMTWR_FUNC_021 | INDEX.md observed; no BRD/FRD entry | No BRD/FRD requirement covers the download/export. BA to clarify whether download is in scope, what format (PDF/Excel/PNG), and what data is included. |
| Hover tooltip absent | TC_SMTWR_FUNC_007 | INDEX.md states no separate tooltip renders | User-supplied prompt mentions "tooltip shows unit info" on hover, but live UI shows only CSS hover state on cell border. Captured as visual-only hover state. If a tooltip is a future requirement, BA must add to FRD. |
| FRD §1.5 vs corrected colour map | All colour TCs (010–015) | SM-FS-Tower-Heatmap §1.5 | Legacy FRD §1.5 table (White/Green, Orange, Red, Blue) is superseded by FSD-CORRECTION 2026-05-25. BA may consider deleting the legacy table from FRD to avoid future confusion. |
| Multi-session WebSocket test | TC_SMTWR_BIZ_025 | SM-FS-Tower-Heatmap §1.6 #3 | Needs active campaign + a second authenticated session (admin or SM) to mutate inventory. Coordinate with QA Agent for test-data fixture. |
| Inactive tower fixture | TC_SMTWR_BIZ_024 | SM-FS-Tower-Heatmap §1.6 #2 | Needs admin-side data: at least one tower flagged inactive. Coordinate with Admin module test data. |

---

## Review Summary

- **Total TCs:** 30
- **By Priority:** P0 = 8 · P1 = 12 · P2 = 7 · P3 = 1 · CONDITIONAL = 3 (TC_012, TC_021, TC_024, TC_025 — 4 entries marked CONDITIONAL pending gap resolution)
- **By Type:** UI = 7 · FUNC = 13 · BIZ = 4 · NEG = 6
- **Visual Evidence Coverage:**
  - TCs citing at least one screenshot from `visual-memory/sm/tower-heatmap/`: 28 / 30
  - TCs without screenshot citation (auth gate behaviours by nature): 2 (TC_028, TC_030)
  - **Visual coverage: 28/30 = 93.3% — exceeds 80% APPROVED threshold**
- **Screenshot utilisation:**
  - `heatmap-loaded.png` — cited in 9 TCs
  - `heatmap-tower-selected.png` — cited in 16 TCs
  - `heatmap-unit-hover.png` — cited in 1 TC
  - `heatmap-unit-click.png` — cited in 4 TCs
  - `heatmap-filter.png` — cited in 1 TC
  - All 5 captured screenshots referenced ≥ 1 time
- **BRD/FRD traceability:** Every TC carries at least one BRD/FRD requirement ID. No orphan TCs.
- **6 unit status classes covered:** available (TC_010), booked (TC_011), pbt (TC_012), refuse-area refuge (TC_013), disabled (TC_014), not-available (TC_015). All 6 covered.
- **Required scenarios covered:**
  - Page load (default tower) — TC_001
  - Tower switching — TC_004, TC_017
  - Unit hover — TC_007
  - Unit click → side panel with Agreement Value/Stamp Duty/GST/Registration Charges — TC_008
  - All 6 unit status types — TC_010 to TC_015
  - Colour legend interpretation — TC_010 to TC_015 + Source-of-Truth Note
  - Read-only nature (allocation via Physical Allocation, not heatmap) — TC_009
  - Floor/unit grid navigation — TC_005, TC_006, TC_016
- **Dual-source gate:** PASSED — `visual-memory/sm/tower-heatmap/INDEX.md` (FULL) + 3 BRD/FRD docs present.
- **LSQ / Strapi / Admin Config:** out of scope — not referenced.
- **Overall Status:** **APPROVED** (with 4 CONDITIONAL TCs awaiting gap resolution as documented above).

Hand-off:
- **Tech Lead Agent:** confirm `locators/sm/locator-map.json` includes `tower-heatmap` module entries — selectors needed: `li.tower-item`, `li.tower-item.selected-tower`, `h6.tower-name-title`, `div.unit-number.available-unit`, `div.unit-number.booked-unit`, `div.unit-number.disabled-unit`, `div.unit-number.refuse-area.refuge-unit`, `div.unit-number.pbt-unit`, `div.unit-number.not-available-unit`, `.unit-size-item`, `.unit-section-box`, `span.preference-count-badge`, Unit Details panel `h5:has-text("Unit Details")`.
- **QA Agent:** run `test-case-reviewer` skill against this file with INDEX.md + 3 BRD/FRD paths to confirm APPROVED status and validate the 4 CONDITIONAL flags. After approval, scaffold POM (`automation-repository/pages/sales-manager/TowerHeatmapPage.js`) and spec (`tests/e2e/sales-manager/tower-heatmap.spec.js`).
