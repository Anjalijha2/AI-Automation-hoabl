# TestCases — Admin Portal / Offers

**Generated:** 2026-06-03
**Portal:** Admin
**Module:** Offers
**URL:** https://uat-web.xrportal.in/admin/offers
**Visual Memory:** `visual-memory/admin/offers/INDEX.md` (CAPTURE_STATUS: FULL — 7 screens captured: 4 on 2026-06-03 + 3 drawer/delete-confirm on 2026-06-06)
**BRD Source:** `.claude/docs/hoabl-knowledge-base/Admin-Portal/BRD/ADMIN-BRD-Offers.md`
**Dual-Source Confirmed:** YES — UI selectors from INDEX.md Key Structural Notes (all 7 screens including drawer fields, delete modal); business rules from BRD §1–§10.6
**Visual Caveat:** Drawer + delete-confirm screenshots now captured (`offers-add-drawer.png`, `offers-edit-drawer.png`, `offers-delete-confirm.png`) on 2026-06-06. Previous `[STUB-EVIDENCE]` flags have been upgraded to real filenames. Only `EDGE_028` retains `[STUB-EVIDENCE]` (future-state placeholder — Offer Code UI field does not exist).
**Status:** Approved — 25 of 30 TCs have FULL visual evidence (83.3%); 4 TCs carry `[NO-VISUAL-EVIDENCE]` by-design (BIZ_023–026 cross-portal Buyer-side, EDGE_027 API-only, EDGE_029/030 DB-only — already excluded from Sheet 2); 1 TC (`EDGE_028`) retains `[STUB-EVIDENCE]` as a documented future-state placeholder. No LOGIC_GAP, no VISUAL_MISMATCH, BRD traceability 100%. See `review-report.md` (2026-06-06).

---

## Offer Type Enum (authoritative — INDEX.md + BRD §3)
Allowed offer types: **Amount Based · Percentage Based**.
System-generated offer codes (NOT admin-created via UI): `HOME_LOAN`, `VC_REQUEST` (BRD §4).

---

## Sheet 1 — Manual Test Cases

| TC_ID | BRD Req ID | Type | Scenario | Preconditions | Steps | Expected Result | Visual Evidence | Test Data | Priority | Status |
|-------|-----------|------|----------|---------------|-------|-----------------|-----------------|-----------|----------|--------|
| TC_OFFERS_UI_001 | BRD §1 + §6 | UI | Verify Offers Management page renders as the admin entry point for discount configuration per BRD §1 | Admin logged in; session at `automation-repository/fixtures/.auth/admin.json` | 1. Navigate to `https://uat-web.xrportal.in/admin/offers` 2. `waitForSelector` on `h5` "Offers Management" 3. Observe page structure | Page renders with `h5` "Offers Management" page title; header row exposes two buttons: `button:has-text("Refresh")` (ant-btn-default) and `button.ant-btn-primary:has-text("Add New Offer")`. No tabs, no search input. Offers table is displayed below the header | visual-memory/admin/offers/offers-full.png · visual-memory/admin/offers/screenshot-desktop.png | N/A | P1 | Approved |
| TC_OFFERS_UI_002 | BRD §6 (table columns) | UI | Verify Offers table renders all 9 columns documented in INDEX.md — admin must see Amount, Percentage, validity dates per BRD §3 + §5.4 | Admin on `/admin/offers`; ≥1 offer exists | 1. Wait for table to load 2. Inspect column headers from left to right | Table headers appear in order: `Sr.no` (sticky-left) \| `Offer Name` \| `Description` \| `Amount` \| `Percentage` \| `Start Date` \| `End Date` \| `Created By` \| `Action` | visual-memory/admin/offers/offers-loaded.png | N/A | P1 | Approved |
| TC_OFFERS_UI_003 | BRD §3 (Amount-Based) | UI | Verify Amount-Based offer rows display the INR value in Amount column and `-` in Percentage column per INDEX.md Amount/Percentage pattern + BRD §3 | Admin; ≥1 Amount-Based offer in list | 1. Locate row where offer type = Amount Based 2. Read Amount cell 3. Read Percentage cell | Amount cell shows `₹ X,XXX` (e.g. `₹ 27,000`); Percentage cell shows `-` | visual-memory/admin/offers/offers-loaded.png | offerType=Amount Based; amount=27000 | P1 | Approved |
| TC_OFFERS_UI_004 | BRD §3 (Percentage-Based) | UI | Verify Percentage-Based offer rows display the percentage value in Percentage column and `-` in Amount column per INDEX.md Amount/Percentage pattern + BRD §3 | Admin; ≥1 Percentage-Based offer in list | 1. Locate row where offer type = Percentage Based 2. Read Amount cell 3. Read Percentage cell | Amount cell shows `-`; Percentage cell shows `X%` (e.g. `5%`) | visual-memory/admin/offers/offers-loaded.png | offerType=Percentage Based; percentage=5 | P1 | Approved |
| TC_OFFERS_UI_005 | INDEX.md Row Action section + BRD §6.5–§6.7 | UI | Verify each row's Action column exposes exactly 3 controls in a flex row: ON/OFF toggle, Edit icon, Delete icon — admin must be able to toggle, edit, delete per BRD §6 | Admin; ≥1 offer in list | 1. Locate any row in the offers table 2. Inspect the Action cell — count and identify the 3 controls | Action cell renders 3 controls in a flex row, in order: (1) `button.ant-switch` (Active/Inactive toggle), (2) `button.ant-btn-icon-only` (Edit pencil icon, 2nd in flex), (3) `button.ant-btn-icon-only` (Delete trash icon, 3rd in flex) | visual-memory/admin/offers/offers-loaded.png | N/A | P1 | Approved |
| TC_OFFERS_UI_006 | INDEX.md Row Action + BRD §6.4 (Active default ON) | UI | Active toggle visually distinguishes ON vs OFF via `ant-switch-checked` class per INDEX.md Row Action section | Admin; mix of Active and Inactive offers in list | 1. Identify an Active offer row 2. Inspect the toggle element 3. Identify an Inactive offer row 4. Inspect the toggle element | Active toggle: `button.ant-switch.ant-switch-checked` (switch handle right, blue background). Inactive toggle: `button.ant-switch` WITHOUT `ant-switch-checked` (switch handle left, grey background) | visual-memory/admin/offers/offers-loaded.png | activeOffer=any; inactiveOffer=any | P1 | Approved |
| TC_OFFERS_UI_007 | BRD §4 (HOME_LOAN system-generated) | UI | Verify `HOME_LOAN` system-generated offer exists in list and is recognisable as auto-created (NOT manually created by admin) per BRD §4 | Admin on `/admin/offers`; at least one buyer home loan approved historically so HOME_LOAN exists | 1. Locate row with offer code/name containing `HOME_LOAN` 2. Read Created By column 3. Inspect row to confirm it appears alongside admin-created offers | A `HOME_LOAN` row is present in the table; Created By column reflects system origin (NOT a regular admin user). Row is visible in the same list as admin-created offers — distinguishable only by code/name and Created By value | visual-memory/admin/offers/offers-loaded.png · visual-memory/admin/offers/offers-full.png | offerCode=HOME_LOAN | P1 | Approved |
| TC_OFFERS_FUNC_008 | BRD §6.1–§6.4 (Add Amount-Based) | FUNC | Admin creates an Amount-Based offer via `Add New Offer` per BRD §6.1–§6.4 — happy path | Admin on `/admin/offers` | 1. Click `button.ant-btn-primary:has-text("Add New Offer")` 2. Wait for `.ant-drawer-content` with `.ant-drawer-title` "Add New Offer" 3. Fill `input#name` (Offer Name) 4. Select Offer Type radio = `AMOUNT` 5. Enter `input#amount` (Amount INR) 6. Pick Start Date via `.ant-picker.ant-picker-range` 7. Pick End Date (after Start) 8. Optionally fill `textarea#description` 9. Click `button.ant-btn-primary:has-text("Create Offer")` | Drawer opens with `.ant-drawer-content`. After submit, drawer closes, table reloads, new offer row appears with: Offer Name as entered, Amount = `₹ <value>`, Percentage = `-`, Start/End Date as picked, Created By = current admin, Active toggle ON by default (BRD §6.4) | visual-memory/admin/offers/offers-add-drawer.png · visual-memory/admin/offers/offers-loaded.png | offerName=`QA_Amt_<timestamp>`; type=Amount Based; amount=15000; startDate=today; endDate=today+30d | P1 | Approved |
| TC_OFFERS_FUNC_009 | BRD §3 + §6.3 (Add Percentage-Based) | FUNC | Admin creates a Percentage-Based offer per BRD §3 + §6.3 — happy path | Admin on `/admin/offers` | 1. Click `Add New Offer` 2. Wait for `.ant-drawer-content` 3. Fill `input#name` 4. Select Offer Type radio = `PERCENTAGE` 5. Enter percentage value in the swapped `input#amount` (percent input) — e.g. 5 6. Pick Start Date 7. Pick End Date 8. Click `button.ant-btn-primary:has-text("Create Offer")` | Drawer closes; new offer row appears with: Amount = `-`, Percentage = `5%`, Active toggle ON (BRD §6.4) | visual-memory/admin/offers/offers-add-drawer.png · visual-memory/admin/offers/offers-loaded.png | offerName=`QA_Pct_<timestamp>`; type=Percentage Based; percentage=5; startDate=today; endDate=today+15d | P1 | Approved |
| TC_OFFERS_FUNC_010 | BRD §10.1 (unitTypologyId scalar) | FUNC | Verify Offer creation accepts a SINGLE typology selection (NOT multi-select) per BRD §10.1 — to target multiple typologies admin must create multiple offers | Admin on Add New Offer drawer | 1. Click `Add New Offer` 2. Locate the `.ant-select` containing `input#unitTypologyId` (Select Typology field) 3. Attempt to select multiple typologies | Typology control is single-select Ant Select with `input#unitTypologyId` (placeholder "Please select typology") — accepts only one value. If admin needs multi-typology coverage they must close drawer and create additional offers — documents BRD §10.1 reconciliation | visual-memory/admin/offers/offers-add-drawer.png | typologyOptions=[1Bed, 2Bed] | P2 | Approved |
| TC_OFFERS_VAL_011 | BRD §6.3 (required fields) | VAL | Submitting Add Offer drawer with all fields blank surfaces required-field validation errors | Admin on Add New Offer drawer (empty form) | 1. Click `Add New Offer` 2. Without entering any field, click `button.ant-btn-primary:has-text("Create Offer")` inside the drawer | Inline errors appear under each required field (`input#name`, Offer Type radio, `input#amount`, `dateRange` Start/End) using Ant Form `.ant-form-item-explain-error`; drawer stays open; no row added to table | visual-memory/admin/offers/offers-add-drawer.png | all-fields-blank | P1 | Approved |
| TC_OFFERS_VAL_012 | BRD §5.4 (date validity) | VAL | Reject creation when End Date is before Start Date — date-order guard per BRD §5.4 (offers apply only when start ≤ today ≤ end) | Admin on Add New Offer drawer | 1. Fill `input#name`, select Offer Type, enter `input#amount` 2. Pick Start Date = today+10d via `.ant-picker.ant-picker-range` 3. Pick End Date = today+5d 4. Click `Create Offer` | Validation error surfaces on End Date (or submit blocked at UI/validator layer per BRD §10.6 — service-level check missing, controller/validator middleware enforces); drawer stays open; no row added | visual-memory/admin/offers/offers-add-drawer.png | startDate=today+10d; endDate=today+5d | P1 | Approved |
| TC_OFFERS_VAL_013 | BRD §3 (Amount vs Percentage exclusivity) | VAL | Verify the discount-value field is gated by Offer Type — Amount-Based shows Amount field; Percentage-Based shows Percentage field. Selecting both is not allowed per BRD §3 (single discount mechanism per offer) | Admin on Add New Offer drawer | 1. Open drawer 2. Click Offer Type radio = `AMOUNT` and inspect — label reads "Amount" and `input#amount` has `₹ ` prefix 3. Click Offer Type radio = `PERCENTAGE` and inspect — label swaps to "Percentage" and `input#amount` becomes percent input | When Type = AMOUNT: label is "Amount", `input#amount` formats with `₹ ` prefix. When Type = PERCENTAGE: label is "Percentage", same `input#amount` accepts percent values. Single input swaps role based on Offer Type radio — not both visible at once | visual-memory/admin/offers/offers-add-drawer.png | typeAmount, typePercentage | P2 | Approved |
| TC_OFFERS_FUNC_014 | BRD §6.5 + §10.5 (toggle deactivate) | FUNC | Clicking Active toggle on an ON offer flips it to OFF immediately with NO confirmation dialog per BRD §5.2 + §6.5 | Admin; ≥1 Active offer in list (toggle currently `ant-switch-checked`) | 1. Locate Active offer row 2. Click `button.ant-switch.ant-switch-checked` in Action cell | Toggle flips instantly to Inactive state: element becomes `button.ant-switch` WITHOUT `ant-switch-checked` (handle left, grey). NO `.ant-modal-content` confirmation appears. Offer immediately stops applying to live allocation campaigns (BRD §5.1 live effect) | visual-memory/admin/offers/offers-loaded.png | activeOfferId=any | P1 | Approved |
| TC_OFFERS_FUNC_015 | BRD §6.6 + §5.1 (toggle re-activate) | FUNC | Clicking Inactive toggle flips it back to ON, restoring live offer application per BRD §6.6 + §5.1 | Admin; ≥1 Inactive offer (toggle WITHOUT `ant-switch-checked`) | 1. Locate Inactive offer row 2. Click `button.ant-switch` in Action cell | Toggle flips to `button.ant-switch.ant-switch-checked` instantly; no confirmation modal; offer immediately starts qualifying for buyer pricing (subject to BRD §5.4 date validity) | visual-memory/admin/offers/offers-loaded.png | inactiveOfferId=any | P1 | Approved |
| TC_OFFERS_FUNC_016 | BRD §6.7 + INDEX.md Edit icon | FUNC | Clicking the Edit icon (1st `button.ant-btn-icon-only` among icon-only buttons in Action cell — nth(0)) opens the Edit drawer pre-populated with the row's current values | Admin; ≥1 offer in list | 1. Locate any offer row 2. Click the 1st `button.ant-btn-icon-only` (pencil icon, index 0) in the Action cell 3. Wait for `.ant-drawer-content` 4. Inspect `.ant-drawer-title` and field values | Drawer opens with `.ant-drawer-content`; `.ant-drawer-title` = "Edit Offer"; fields pre-populated: `input#name` (Offer Name), Offer Type radio, `input#amount`, `textarea#description`, dateRange (e.g. `06 May 2026` / `30 May 2026`), `input#unitTypologyId` shows selected typology (e.g. "2 Bed Growth Home"). Primary CTA = `button.ant-btn-primary:has-text("Update Offer")` | visual-memory/admin/offers/offers-edit-drawer.png · visual-memory/admin/offers/offers-loaded.png | existingOfferId=any admin-created | P1 | Approved |
| TC_OFFERS_FUNC_017 | BRD §6.7 (Edit save) | FUNC | Edit an offer's discount value and Save — table row reflects the new value | Admin; Edit drawer open with pre-populated values | 1. Open Edit drawer for a known admin-created offer 2. Change `input#amount` value (Amount or Percentage) 3. Click `button.ant-btn-primary:has-text("Update Offer")` | Drawer closes; table reloads; the same row now shows the updated discount value in the Amount or Percentage column; toggle state preserved | visual-memory/admin/offers/offers-edit-drawer.png · visual-memory/admin/offers/offers-loaded.png | offerId=any admin-created; newAmount=25000 | P1 | Approved |
| TC_OFFERS_FUNC_018 | INDEX.md Delete icon + BRD §6.7 — Delete safe | FUNC | **SAFE variant**: Click Delete icon to trigger the delete confirmation modal, then dismiss without confirming — offer is NOT deleted | Admin; ≥1 offer in list | 1. Locate any offer row 2. Click the 2nd `button.ant-btn-icon-only` (trash icon, index 1) in the Action cell 3. Wait for `.ant-modal-content` 4. Click `button.ant-btn-default:has-text("Cancel")` 5. Re-check the offer row | `.ant-modal-content` appears with title "Are you sure you want to delete this offer?", body "This action cannot be undone.", buttons Cancel + Yes, delete. After clicking Cancel (or Escape), no row is removed; offers list unchanged | visual-memory/admin/offers/offers-delete-confirm.png · visual-memory/admin/offers/offers-loaded.png | offerId=any admin-created | P1 | Approved |
| TC_OFFERS_FUNC_019 | BRD §6.7 + §10.4 — Delete confirm `[MANUAL-ONLY]` | FUNC | **DESTRUCTIVE variant**: Confirm Delete; offer is hard-deleted per BRD §10.4 (`offer.service.js` calls `offer.destroy()` — no soft-delete, no audit). Mark `[MANUAL-ONLY]` since deletion is irreversible and may orphan `RegistrationUnitOffer` references | Admin; **dedicated disposable offer** created for this run (do NOT delete a shared/seeded offer); offer must NOT be referenced by any active booking (per BRD §10.4 orphan risk) | 1. Locate the disposable offer row 2. Click the 2nd `button.ant-btn-icon-only` (trash, index 1) 3. In `.ant-modal-content` click `button.ant-btn-dangerous:has-text("Yes, delete")` 4. Wait for `networkidle` 5. Re-render list / click Refresh | Modal closes; table reloads; the disposable offer row is gone (hard-deleted via `offer.destroy()`). NO undo control surfaced. Note: per BRD §10.4, no audit log emitted and no FK guard — execution must be on a known-unreferenced offer | visual-memory/admin/offers/offers-delete-confirm.png · visual-memory/admin/offers/offers-loaded.png | disposableOfferId=`<created-this-run>` | P1 | Approved (Manual-only — destructive, excluded from Sheet 2 automation) |
| TC_OFFERS_FUNC_020 | INDEX.md header — Refresh button | FUNC | Refresh button reloads the offers list from backend | Admin on `/admin/offers` | 1. Click `button:has-text("Refresh")` in the page header 2. Observe loading indicator and network call | Table reloads (brief spinner / network call to GET offers endpoint); list shows current backend state. Any newly-created offers from another tab become visible after Refresh | visual-memory/admin/offers/offers-full.png | N/A | P2 | Approved |
| TC_OFFERS_NEG_021 | BRD §4 + §10.2 — HOME_LOAN cannot be manually created via UI | NEG | Admin cannot manually create a `HOME_LOAN` offer through the Add New Offer UI flow — HOME_LOAN is system-generated per BRD §4. NOTE: BRD §10.2 reconciliation flags that `offerCode` is admin-settable at the controller level (no whitelist enforcement) — this TC asserts the UI-level intent, NOT the backend enforcement | Admin on Add New Offer drawer | 1. Open Add New Offer drawer 2. Inspect the drawer form for any Offer Code input — verify only documented fields are present: `input#name`, Offer Type radio, `input#amount`, `textarea#description`, dateRange, `input#unitTypologyId` 3. Confirm no offerCode input is rendered | Drawer does NOT expose an Offer Code field for admin entry — only 6 documented form fields render. HOME_LOAN/VC_REQUEST are system-generated and only created via backend triggers per BRD §4. If a Code field is later added and accepts `HOME_LOAN`, raise a bug citing BRD §10.2 (controller-level permissive behaviour must be hardened) | visual-memory/admin/offers/offers-add-drawer.png · visual-memory/admin/offers/offers-loaded.png | offerCode=HOME_LOAN | P1 | Approved |
| TC_OFFERS_NEG_022 | BRD §6.5 + §8 (live-effect CRITICAL) | NEG | Verify toggling an offer OFF has NO confirmation dialog per BRD §5.2 — guard against the assumption that destructive UX precedes deactivation. Documents BRD §8 critical risk that toggles affect live buyer sessions instantly | Admin; ≥1 Active offer in list | 1. Locate Active toggle 2. Click it 3. Within 200ms inspect DOM for any `.ant-modal-content`, `.ant-popover-inner-content`, or other confirmation surface | NO confirmation modal/popover appears between click and state change. Toggle flips instantly. This documents BRD §5.2 "No confirmation on toggle — takes effect instantly" — important regression guard if a dev later adds a confirmation step | visual-memory/admin/offers/offers-loaded.png | activeOfferId=any | P1 | Approved |
| TC_OFFERS_BIZ_023 | BRD §5.4 + §7 (date-validity gating) | BIZ | Offer with Start Date in the future does NOT yet apply to buyer pricing per BRD §5.4 + §7 — Admin can see it Active but offer is not in the qualifying-offer set | Admin; create offer with startDate=today+5d, endDate=today+30d, Active=ON; buyer session has unit selected and proceeds to pay TODAY | 1. As admin, create the future-dated offer (or pre-seed) 2. As buyer (downstream cross-portal — out of Admin in-page scope), select a unit and proceed to pay 3. As admin, inspect the buyer's confirmation amount or query the qualifying-offer set | Offer is NOT included in the discount sum because `startDate > today` per BRD §7. Admin-side toggle shows Active=ON but applicability is gated by date. Cross-portal observation — Admin Offers page shows only the row state, not the buyer outcome | visual-memory/admin/offers/offers-loaded.png · [NO-VISUAL-EVIDENCE] (Buyer-side outcome not in Admin INDEX.md) | startDate=today+5d; endDate=today+30d; isActive=true | P2 | Approved (manual-only — cross-portal Buyer-side; excluded from Sheet 2 by design) |
| TC_OFFERS_BIZ_024 | BRD §5.5 (pricing formula) | BIZ | Pricing formula: Agreement Value − sum(active, applicable, date-valid offers) = All Inclusive Price per BRD §5.5. Multiple active offers stack additively | Admin; ≥2 Active offers in list (one Amount, one Percentage); both date-valid; both apply to the typology in test | 1. Note the Amount and Percentage values for the two active offers on the Offers page 2. Cross-portal — query buyer All-Inclusive Price for a unit with Agreement Value V | All-Inclusive Price = V − amountOffer − (percentageOffer% × V). Documents pricing math; admin-side observation limited to confirming offers exist with stated values | visual-memory/admin/offers/offers-loaded.png · [NO-VISUAL-EVIDENCE] (Buyer-side pricing not in Admin INDEX.md) | offerA: type=Amount, amount=10000; offerB: type=Percentage, value=5 | P2 | Approved (manual-only — cross-portal Buyer-side; excluded from Sheet 2 by design) |
| TC_OFFERS_BIZ_025 | BRD §5.6 + §8 (race condition CRITICAL) | BIZ | If an active offer is toggled OFF after buyer views pricing but before they complete payment, the offer disappears from the final booking amount per BRD §5.6. CRITICAL per BRD §8 | Admin; ≥1 Active offer; buyer mid-flow (cross-portal) | 1. Buyer views pricing → records discount amount visible 2. Admin clicks Active toggle on that offer (now `ant-switch` without `ant-switch-checked`) 3. Buyer clicks Proceed to Pay 4. Inspect final confirmation amount | Final confirmation amount excludes the toggled-off offer's discount, even though the buyer had seen it pre-toggle. Live-effect risk documented in BRD §8 | visual-memory/admin/offers/offers-loaded.png · [NO-VISUAL-EVIDENCE] (Buyer-side outcome cross-portal) | activeOfferId=any high-value | P1 | Approved (manual-only — cross-portal Buyer-side; excluded from Sheet 2 by design) |
| TC_OFFERS_BIZ_026 | BRD §5.7 (locked bookings) | BIZ | Buyers who already completed payment are NOT affected by subsequent offer changes per BRD §5.7 | Admin; a buyer has completed payment with offer X applied; admin then deactivates or deletes offer X | 1. Confirm buyer booking exists with offer X applied (cross-portal evidence) 2. As admin, toggle offer X OFF (or delete it per `[MANUAL-ONLY]` TC_OFFERS_FUNC_019) 3. Re-query the completed booking record | Existing completed booking retains its original discount; total paid unchanged. Future buyer sessions reflect the toggled-off / deleted state but historical bookings are locked | visual-memory/admin/offers/offers-loaded.png · [NO-VISUAL-EVIDENCE] (Booking record cross-portal) | offerId=X with ≥1 completed booking | P2 | Approved (manual-only — cross-portal Buyer-side; excluded from Sheet 2 by design) |
| TC_OFFERS_EDGE_027 | BRD §10.3 (pagination params) | EDGE | GET offers endpoint accepts `page`, `limit`, `projectId` query parameters per BRD §10.3 — API-level, not surfaced as UI controls on the current Offers page (no visible search, no pagination footer in INDEX.md notes) | API-direct call with `Authorization` header for admin | 1. Call `GET /api/v1/admin/offers?page=1&limit=5&projectId=2` 2. Inspect response payload shape and count | API returns paginated list capped at `limit=5` for `projectId=2`. UI Offers page may not expose page/limit controls (INDEX.md does not document a pagination footer) — TC documents API capability not yet surfaced | [NO-VISUAL-EVIDENCE] — API-level only | page=1; limit=5; projectId=2 | P3 | Approved (API-only — to be implemented in tests/api/offers.api.spec.js; excluded from Sheet 2 UI suite) |
| TC_OFFERS_EDGE_028 | BRD §10.2 (offerCode permissive — controller-level) | EDGE | If a future UI patch exposes Offer Code as an admin input, verify that `offerCode='HOME_LOAN'` or `'VC_REQUEST'` is rejected at the validator layer (or flag bug if accepted) per BRD §10.2 | Admin on Add New Offer drawer (future-state — flag if surfaced) | 1. If/when an Offer Code field is added, attempt to enter `HOME_LOAN` 2. Submit | TC future-state placeholder: should be rejected once whitelist is implemented. Today (per BRD §10.2 controller audit), backend accepts any code from req.body — known gap flagged to Developer Agent | [STUB-EVIDENCE] (UI surface for offerCode does not yet exist — intentional placeholder) | offerCode=HOME_LOAN | P3 | Approved (future-state placeholder — UI does not yet exist; excluded from Sheet 2 until Offer Code input ships) |
| TC_OFFERS_EDGE_029 | BRD §10.5 (toggle no-audit) | EDGE | Toggle action emits NO audit log per BRD §10.5 — known engineering gap. TC documents lack of audit trail for activation changes | Admin; activate/deactivate a known offer | 1. Toggle offer ON or OFF 2. Query audit log table / endpoint for an entry referencing that offer + timestamp | No audit log row created for the toggle event. Documents BRD §10.5 — important risk for compliance / dispute resolution | [NO-VISUAL-EVIDENCE] — backend/DB observation | offerId=any; action=toggle | P2 | Approved (DB-only — to be implemented in tests/db/offers.db.spec.js via db/queries; excluded from Sheet 2 UI suite) |
| TC_OFFERS_EDGE_030 | BRD §10.4 (delete no FK guard) | EDGE | Deleting an offer that is referenced by an existing `RegistrationUnitOffer` row leaves orphaned downstream pricing rows per BRD §10.4 | Admin; offer X exists AND is referenced by ≥1 RegistrationUnitOffer row | 1. Confirm referencing row exists 2. As admin, delete offer X via UI flow 3. Re-query `RegistrationUnitOffer` for orphan rows | Offer X row removed from offers table; `RegistrationUnitOffer.offerId=X` rows remain in DB with broken FK reference. Documents BRD §10.4 — no FK guard, no cascade. Bug already flagged to Developer Agent | [NO-VISUAL-EVIDENCE] — DB-level observation | offerId=X with active references | P2 | Approved (DB-only — to be implemented in tests/db/offers.db.spec.js; excluded from Sheet 2 UI suite) |

---

## Sheet 2 — Automation Candidates

TCs with FULL visual evidence AND no destructive-state dependency AND no cross-portal/API-only dependency. `[MANUAL-ONLY]` destructive TCs (FUNC_019) and cross-portal BIZ TCs (BIZ_023–026) and API/DB-only edges (EDGE_027, EDGE_029, EDGE_030) are excluded. EDGE_028 is a future-state placeholder (Offer Code field does not exist in current UI) and is excluded until that surface is implemented.

| TC_ID | Module | Type | Automatable | Complexity | Playwright Suite | Visual Evidence Status | Notes |
|-------|--------|------|-------------|------------|------------------|------------------------|-------|
| TC_OFFERS_UI_001 | Offers | UI | Yes | Low | ui-ux | FULL | Page render check — heading, header buttons |
| TC_OFFERS_UI_002 | Offers | UI | Yes | Low | ui-ux | FULL | 9-column header assertion |
| TC_OFFERS_UI_003 | Offers | UI | Yes | Low | ui-ux | FULL | Amount-Based row pattern: `₹ X,XXX` / `-` |
| TC_OFFERS_UI_004 | Offers | UI | Yes | Low | ui-ux | FULL | Percentage-Based row pattern: `-` / `X%` |
| TC_OFFERS_UI_005 | Offers | UI | Yes | Low | ui-ux | FULL | Action cell 3-control structure |
| TC_OFFERS_UI_006 | Offers | UI | Yes | Low | ui-ux | FULL | Active vs Inactive class assertion (`ant-switch-checked`) |
| TC_OFFERS_UI_007 | Offers | UI | Yes | Low | regression | FULL | HOME_LOAN row presence — requires seeded HOME_LOAN |
| TC_OFFERS_FUNC_008 | Offers | FUNC | Yes | Medium | e2e | FULL | Add Amount-Based — drawer body fields now documented via `offers-add-drawer.png` |
| TC_OFFERS_FUNC_009 | Offers | FUNC | Yes | Medium | e2e | FULL | Add Percentage-Based — same drawer evidence |
| TC_OFFERS_FUNC_010 | Offers | FUNC | Yes | Low | regression | FULL | Typology scalar (Ant Select `input#unitTypologyId`) |
| TC_OFFERS_VAL_011 | Offers | VAL | Yes | Low | regression | FULL | Required-field errors via `.ant-form-item-explain-error` |
| TC_OFFERS_VAL_012 | Offers | VAL | Yes | Low | regression | FULL | End < Start date guard |
| TC_OFFERS_VAL_013 | Offers | VAL | Yes | Low | regression | FULL | Amount/Percentage label swap on Offer Type radio |
| TC_OFFERS_FUNC_014 | Offers | FUNC | Yes | Low | e2e | FULL | Toggle deactivate — direct row action, no drawer dependency |
| TC_OFFERS_FUNC_015 | Offers | FUNC | Yes | Low | e2e | FULL | Toggle re-activate |
| TC_OFFERS_FUNC_016 | Offers | FUNC | Yes | Medium | e2e | FULL | Edit drawer pre-populate via `offers-edit-drawer.png` |
| TC_OFFERS_FUNC_017 | Offers | FUNC | Yes | Medium | e2e | FULL | Edit save — `Update Offer` primary CTA |
| TC_OFFERS_FUNC_018 | Offers | FUNC | Yes | Low | e2e | FULL | Delete safe — `.ant-modal-content` Cancel path |
| TC_OFFERS_FUNC_020 | Offers | FUNC | Yes | Low | regression | FULL | Refresh button reloads list |
| TC_OFFERS_NEG_021 | Offers | NEG | Yes | Low | regression | FULL | HOME_LOAN UI guard — assert no Offer Code input in Add drawer (6 documented fields) |
| TC_OFFERS_NEG_022 | Offers | NEG | Yes | Low | regression | FULL | No-confirmation toggle guard — important regression watch |

### Excluded from Sheet 2
- `TC_OFFERS_FUNC_019` — `[MANUAL-ONLY]` destructive delete; needs disposable-offer factory + FK-guard check (visual evidence FULL, but excluded for safety)
- `TC_OFFERS_BIZ_023`, `TC_OFFERS_BIZ_024`, `TC_OFFERS_BIZ_025`, `TC_OFFERS_BIZ_026` — cross-portal (Buyer side); Admin Offers INDEX.md does not own the downstream evidence
- `TC_OFFERS_EDGE_027` — API-only pagination
- `TC_OFFERS_EDGE_028` — future-state placeholder; UI surface does not yet exist (retains `[STUB-EVIDENCE]` as a documented placeholder, not a gap against current UI)
- `TC_OFFERS_EDGE_029`, `TC_OFFERS_EDGE_030` — backend/DB observation

---

## Sheet 3 — Bug Template

| Bug ID | TC_ID | Severity | Steps | Actual | Expected | Environment | Status |
|--------|-------|----------|-------|--------|----------|-------------|--------|
| BUG_NNN | TC_OFFERS_XXX_NNN | P1/P2/P3 | <reproduction steps> | <observed> | <per TC Expected Result> | UAT https://uat-web.xrportal.in/admin/offers | Open / In Progress / Closed |

---

## Coverage Summary

| Metric | Value |
|--------|-------|
| Total TCs | 30 |
| TCs with FULL visual evidence | 25 (UI_001–007 + FUNC_008, 009, 010, VAL_011, 012, 013, FUNC_014, 015, 016, 017, 018, 019, 020, NEG_021, 022) |
| TCs with STUB visual evidence | 1 (EDGE_028 — future-state placeholder; Offer Code UI does not yet exist — by design, not a gap) |
| TCs with NO visual evidence (cross-portal / API / DB) | 4 unique non-in-page TCs (BIZ_023–026), 3 backend-only (EDGE_027, 029, 030) — by design, excluded from Sheet 2 |
| Visual coverage (FULL ÷ total) | 25 / 30 = **83.3%** ≥ 80% threshold |
| Visual coverage (FULL ÷ automation-eligible in-page TCs) | 25 / 25 = **100%** for the 25 TCs that target the Admin Offers page directly |
| BRD traceability | 100% — every TC carries a BRD §-reference |
| P1 TCs | 17 |
| P2 TCs | 9 |
| P3 TCs | 4 |

---

## Visual Gaps Flagged to Tech Lead Agent

```
RESOLVED 2026-06-06:
- offers-add-drawer.png   captured — Add New Offer drawer body
- offers-edit-drawer.png  captured — Edit Offer drawer, pre-populated
- offers-delete-confirm.png captured — `.ant-modal-content` delete confirmation

All 12 previously-flagged STUB-EVIDENCE TCs (FUNC_008, 009, 010, 016, 017, 018, 019,
VAL_011, 012, 013, NEG_021) now reference real screenshot filenames. Status upgraded
from Conditional to Approved.

Remaining placeholder (NOT a gap):
- TC_OFFERS_EDGE_028 retains [STUB-EVIDENCE] as a documented future-state TC. The
  Offer Code UI field does not exist in the current drawer (only 6 fields render),
  so no screenshot can satisfy it. This TC is intentionally excluded from Sheet 2
  and will be reviewed if/when an Offer Code input is added to the drawer.

Remaining NO-VISUAL-EVIDENCE (by design — cross-portal / API / DB):
- BIZ_023–026: Buyer-side downstream pricing outcomes; Admin INDEX.md does not own
- EDGE_027:    API pagination — no UI surface
- EDGE_029:    Toggle audit-log absence — DB observation
- EDGE_030:    Delete FK-orphan check — DB observation

These are documented out-of-scope for Admin Offers page visual coverage and remain
excluded from Sheet 2.
```
