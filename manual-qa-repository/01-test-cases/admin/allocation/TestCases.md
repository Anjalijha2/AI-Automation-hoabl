# TestCases — Admin Portal / Allocation

**Generated:** 2026-06-02 (re-run, supersedes Conditional batch of same date)
**Portal:** Admin
**Module:** Allocation
**URL:** https://uat-web.xrportal.in/admin/allocation
**Visual Memory:** `visual-memory/admin/allocation/INDEX.md` (CAPTURE_STATUS: FULL — 9 screens, captured 2026-06-01/02)
**BRD/FRD Source:** `.claude/docs/hoabl-knowledge-base/Admin-Portal/BRD/ADMIN-BRD-Allocation.md`
**Dual-Source Confirmed:** YES — UI selectors from INDEX.md Key Structural Notes (all 9 screens); business rules from BRD §1–§10.26
**Supersedes:** previous Conditional batch (34 TCs, 79.4% visual coverage). All 7 NO-VISUAL gaps are now closed.

---

## Campaign Status Enum (authoritative — INDEX.md Status Filter section)
Allowed campaign-list statuses: **Active · Upcoming · Completed · Stopped · Cancelled · Failed** (+ filter pseudo-option "All Status").
NOT a campaign status: `Approved` (that is a buyer-side / Customers-module concept and never appears in the campaign-list context).

---

## Sheet 1 — Manual Test Cases

| TC_ID | BRD Req ID | Type | Scenario | Preconditions | Steps | Expected Result | Visual Evidence | Test Data | Priority | Status |
|-------|-----------|------|----------|---------------|-------|-----------------|-----------------|-----------|----------|--------|
| TC_ALLOC_UI_001 | BRD §1 + §6.2 | UI | Verify Allocation page renders with two stacked sections (New Allocation Campaign form + Campaign list) per BRD §6.2 — admin entry point for campaign management | Admin logged in; session at `automation-repository/fixtures/.auth/admin.json` | 1. Navigate to `https://uat-web.xrportal.in/admin/allocation` 2. `waitForSelector` on `h5` "Allocation" 3. Observe page structure | Page renders with `h5` "Allocation" page title at top, then `h5` "New Allocation Campaign" section heading with Formik form (`.form-section-wrapper.formik-section > form.ant-form.ant-form-vertical`), then filter bar, then empty Campaign table with columns `Campaign Name \| Allocation Type \| Start Time \| End Time \| Status \| Actions` and empty-state text "Please select a project to view campaigns" | visual-memory/admin/allocation/screenshot-desktop.png | N/A | P1 | Pending |
| TC_ALLOC_UI_002 | BRD §6.2 (form fields) | UI | Verify all required form fields are rendered as documented in INDEX.md Form section — BRD §6.3 mandates name/type/start/end inputs | Admin on `/admin/allocation` | 1. Observe New Allocation Campaign form 2. Verify each `.ant-row.ant-form-item-row` field row 3. Note `*` markers on required labels | Form shows 5 required fields each marked `*`: Project (`.ant-select.ant-select-lg.fix-select-input-border` placeholder "Select Project"), Campaign Name (`input.ant-input.ant-input-lg[placeholder="Enter campaign name..."]`), Allocation Type (Ant Select default "Static"), Start Time IST (`input[placeholder="Select date"]` + calendar icon), End Time IST (`input[placeholder="Select date"][disabled]` + calendar icon). One optional Description textarea (`<textarea class="ant-input">`) with `0 / 255` charcount. Two action buttons: Reset (`button.btn-book-outline`) + Save Campaign (`button.btn-book-solid.ant-btn-primary`) | visual-memory/admin/allocation/screenshot-desktop.png · visual-memory/admin/allocation/screenshot-ui.png | N/A | P1 | Pending |
| TC_ALLOC_UI_003 | BRD §6.2 (filter bar) | UI | Verify filter bar above campaign list shows all documented controls — BRD §6.6 admin must filter by project/status/type/name | Admin on `/admin/allocation` | 1. Observe section above empty campaign table 2. Inspect each control | Filter bar renders: Project filter (`.ant-select` placeholder "Select Project"), Status filter (`.ant-select` default "All Status" — disabled until project selected), Type filter (`.ant-select` default "All Types" — disabled until project selected), Campaign name search (`input.ant-input.ant-input-lg[placeholder="Search by campaign name..."]`), Refresh button (disabled until project selected) | visual-memory/admin/allocation/screenshot-desktop.png | N/A | P2 | Pending |
| TC_ALLOC_UI_004 | BRD §6 (sidebar nav) | UI | Verify sidebar navigation lists all expected modules with Allocation active per INDEX.md sidebar notes | Admin on `/admin/allocation` | 1. Observe left sidebar 2. Verify entry list and active state | Sidebar lists: Customers · Config · Allocation (active/highlighted) · Offers · Towers · JBP Mgmt · Channel Partners · Sales Managers · Transactions · CMS | visual-memory/admin/allocation/screenshot-desktop.png | N/A | P3 | Pending |
| TC_ALLOC_UI_005 | INDEX.md Status Filter section | UI | Verify campaign Status filter dropdown enumerates only the documented statuses — guards against unauthorised "Approved" state appearing in campaign-list context (BRD §5 status flow) | Admin on `/admin/allocation`; Project selected in filter bar | 1. Open Status filter Ant Select 2. Read all option labels | Status filter exposes exactly: `All Status`, `Active`, `Upcoming`, `Completed`, `Stopped`, `Cancelled`, `Failed`. NO `Approved` option is shown (that is a buyer-allocation state, not a campaign state) | visual-memory/admin/allocation/screenshot-desktop.png | filter.projectId=selected | P1 | Pending |
| TC_ALLOC_FUNC_006 | BRD §6.3 + §6.4 (Static happy path) | FUNC | Admin creates a STATIC campaign with 3+ minutes lead time → status Upcoming per BRD §4 Rule 1 and §5 status flow | Admin logged in; ≥1 project; ≥1 tower Active in Config CMS (BRD §4 Rule 3) | 1. Open Project select (`.ant-select.ant-select-lg.fix-select-input-border`), choose project 2. Type campaign name into `input[placeholder="Enter campaign name..."]` 3. Confirm Allocation Type Ant Select reads "Static" (default) 4. Open Start Time calendar, pick date/time ≥ now+3min 5. Open End Time calendar (now enabled), pick time > Start 6. Optionally fill Description textarea (charcount updates) 7. Click `button.btn-book-solid.ant-btn-primary` "Save Campaign" | Campaign row appears in Campaign table with status `Upcoming`; form clears; empty-state text replaced by the new row showing Campaign Name, Allocation Type=Static, Start Time, End Time, Status=Upcoming, Actions | visual-memory/admin/allocation/screenshot-desktop.png | projectId=`<UAT_DEFAULT_PROJECT>`; campaignName=`QA_Static_<timestamp>`; type=Static; startTime=now+5min; endTime=now+20min; description="QA smoke" | P1 | Pending |
| TC_ALLOC_FUNC_007 | BRD §3 + §6.3 (Dynamic) | FUNC | Admin creates a DYNAMIC campaign — round-based assignment per BRD §3 | Admin logged in; project available | 1. Open Project select, choose project 2. Type campaign name 3. Open Allocation Type Ant Select; pick "Dynamic" 4. Pick Start Time ≥ now+3min 5. Pick End Time > Start 6. Click "Save Campaign" | Campaign row appears with `Allocation Type = Dynamic`, status `Upcoming` in the campaign table | visual-memory/admin/allocation/screenshot-desktop.png | campaignName=`QA_Dynamic_<timestamp>`; type=Dynamic; startTime=now+5min; endTime=now+25min | P1 | Pending |
| TC_ALLOC_FUNC_008 | BRD §3 + §10.2 (PHYSICAL_EVENT) | FUNC | Admin creates a PHYSICAL_EVENT campaign — walk-in event flow per BRD §3; commonPoolExcel mandatory per §10.2 | Admin logged in; commonPoolExcel file prepared | 1. Open Project select 2. Enter campaign name 3. Select Allocation Type = "Physical Event" 4. Pick Start/End times (3+ min lead) 5. Provide `commonPoolExcel` file (BRD §10.2) 6. Click "Save Campaign" | Campaign row appears with Allocation Type=Physical Event, status Upcoming | visual-memory/admin/allocation/screenshot-desktop.png | type=Physical Event; commonPoolExcel=`samples/physical-event-pool.xlsx` | P1 | Pending |
| TC_ALLOC_VAL_009 | INDEX.md form-validation-errors capture + BRD §6.3 | VAL | Submitting blank form surfaces 4 inline required-field error messages — derived from `allocation-form-validation-errors.png` | Admin on `/admin/allocation` with form empty | 1. Without entering anything, click `button.btn-book-solid.ant-btn-primary` "Save Campaign" 2. Observe inline errors rendered in `.ant-form-item-explain-error` under each control | Four red inline errors appear: under Project — "Project is required"; under Campaign Name — "Campaign name is required"; under Start Time — "Start time is required"; under End Time — "End time is required". Allocation Type and Description do NOT error (Type has default "Static"; Description is optional). Form is NOT submitted | visual-memory/admin/allocation/allocation-form-validation-errors.png | all-fields-blank | P1 | Pending |
| TC_ALLOC_VAL_010 | BRD §4 Rule 1 (3-min lead) | VAL | Reject campaign creation when Start Time < 3 minutes in future per BRD §4 Rule 1 | Admin on form | 1. Fill all fields validly except Start Time 2. Pick Start Time within next 2 minutes 3. Click "Save Campaign" | Form rejects submission with validation error referencing 3-minute lead requirement; campaign NOT created (table empty-state remains) | visual-memory/admin/allocation/allocation-form-validation-errors.png | startTime=now+1min | P1 | Pending |
| TC_ALLOC_VAL_011 | BRD §6.3 (required) | VAL | Reject submission when Campaign Name is empty — required field marked `*` | Admin on form | 1. Select Project 2. Leave Campaign Name empty 3. Fill remaining required fields 4. Click "Save Campaign" | Ant Form shows "Campaign name is required" under the field (red `.ant-form-item-explain-error`); submit blocked | visual-memory/admin/allocation/allocation-form-validation-errors.png | campaignName="" | P1 | Pending |
| TC_ALLOC_VAL_012 | BRD §6.3 (required) | VAL | Reject submission when Project is unselected | Admin on form | 1. Leave Project select blank 2. Fill Campaign Name, Type, Start, End 3. Click "Save Campaign" | Ant Form shows "Project is required" under the Project field; submit blocked. Per BRD §10.1, backend may silently substitute project=2 on UAT via API — UI still enforces required marker | visual-memory/admin/allocation/allocation-form-validation-errors.png | projectId=null | P1 | Pending |
| TC_ALLOC_VAL_013 | BRD §6.3 (chronology guard) | VAL | End Time picker remains disabled until Start Time chosen — chronology guard per INDEX.md Form note | Admin on form | 1. Before picking Start Time, attempt to focus/open End Time input 2. Inspect End Time input attribute | End Time input is `input[placeholder="Select date"][disabled]` with `aria-disabled="true"`; calendar icon inactive; cannot be opened | visual-memory/admin/allocation/screenshot-desktop.png | N/A | P2 | Pending |
| TC_ALLOC_VAL_014 | BRD §6.3 (chronology) | VAL | Reject End Time earlier than Start Time | Admin on form | 1. Pick Start Time = now+10min 2. Pick End Time = now+5min 3. Click "Save Campaign" | Ant Form shows validation error on End Time; submit blocked | visual-memory/admin/allocation/allocation-form-validation-errors.png | startTime=now+10min; endTime=now+5min | P1 | Pending |
| TC_ALLOC_VAL_015 | INDEX.md Form note (textarea charcount) | VAL | Description charcount UI behaves as documented (`0 / 255`) | Admin on form | 1. Type 100 chars into Description textarea 2. Type 255 chars 3. Attempt 256 | Charcount label updates to "100 / 255" then "255 / 255"; further input blocked OR validation error at 256 | visual-memory/admin/allocation/screenshot-desktop.png | description=`<100-char>`, `<255-char>`, `<256-char>` | P3 | Pending |
| TC_ALLOC_FUNC_016 | BRD §6.6 + INDEX.md filter notes | FUNC | Filter bar Status and Type controls enable only after Project filter is selected — gated state | Admin on `/admin/allocation`; ≥1 campaign exists | 1. Observe Status and Type filters before selecting project 2. Select Project in filter bar 3. Re-observe Status and Type filters | Initially Status (default "All Status"), Type (default "All Types"), and Refresh are disabled (`aria-disabled="true"`); after Project selected, all three become enabled | visual-memory/admin/allocation/screenshot-desktop.png | filter.projectId=selected | P2 | Pending |
| TC_ALLOC_FUNC_017 | BRD §6.6 (search) | FUNC | Search by campaign name returns matching rows | Admin; campaign list populated for selected project | 1. Select Project in filter bar 2. Type known fragment in `input[placeholder="Search by campaign name..."]` 3. Allow debounce / press Enter | Campaign table filters to rows whose Campaign Name column contains the fragment | visual-memory/admin/allocation/screenshot-desktop.png | search="QA_Static" | P2 | Pending |
| TC_ALLOC_FUNC_018 | BRD §6.6 (search empty-state) | FUNC | Search with non-matching string surfaces the empty-state placeholder, not the column-header empty | Admin; Project selected; some campaigns exist | 1. Select Project 2. Type `ZZZNOMATCH99999` in `input[placeholder="Search by campaign name..."]` 3. Press Enter | Table body switches to Ant `.ant-empty` placeholder with text "No campaigns found"; pagination footer hidden (no `Total N campaigns` row). Search input shows clear (`×`) icon in `.ant-input-suffix` on focus | visual-memory/admin/allocation/allocation-empty-state.png | search="ZZZNOMATCH99999" | P2 | Pending |
| TC_ALLOC_FUNC_019 | BRD §6.6 (refresh) | FUNC | Refresh button re-pulls campaign list for selected project | Admin; Project selected | 1. Click Refresh (enabled after Project selected) 2. Observe loading then refreshed list | Table reloads (brief spinner / network call); list shows current backend state | visual-memory/admin/allocation/screenshot-desktop.png | projectId=selected | P2 | Pending |
| TC_ALLOC_FUNC_020 | BRD §6.4 + §5 (Upcoming→Active) | FUNC | Campaign transitions Upcoming→Active automatically at Start Time per BRD §5 status flow + §6.5 | Campaign already created with Start Time = now+3min; admin watching list | 1. Wait until current time crosses Start Time 2. Click Refresh in filter bar | Campaign row status column updates from `Upcoming` to `Active` | visual-memory/admin/allocation/screenshot-desktop.png | startTime=now+3min | P1 | Pending |
| TC_ALLOC_UI_021 | INDEX.md Active-row Actions section | UI | Active row Actions column exposes exactly two controls: `View` (anchor) and `Stop` (Ant link-dangerous button). Asserts no Cancel control on Active rows (BRD §4 Rule 5) | Admin; ≥1 Active campaign in list (UAT id 289 `PE QA : Camp Test 4`) | 1. Filter by Status `Active` 2. Locate Active campaign row 3. Inspect Actions column | Actions cell contains exactly `<a>:has-text("View")` (eye icon) and `<button class="ant-btn ant-btn-link ant-btn-dangerous">:has-text("Stop")` (stop-sign icon, red). No `Cancel` button is present. Stop is a row-level action — NOT on the campaign detail page | visual-memory/admin/allocation/allocation-stop-modal.png · visual-memory/admin/allocation/screenshot-desktop.png | activeCampaignId=`289` | P1 | Pending |
| TC_ALLOC_UI_022 | INDEX.md Upcoming-row Actions section | UI | Upcoming row Actions column exposes exactly two controls: `View` (anchor) and `Cancel` (Ant link-dangerous button). Asserts no Stop on Upcoming rows (BRD §4 Rule 5) | Admin; ≥1 Upcoming campaign in list (e.g. "Test" STATIC Upcoming) | 1. Filter by Status `Upcoming` 2. Locate row 3. Inspect Actions column | Actions cell contains exactly `<a>:has-text("View")` and `<button>:has-text("Cancel")`. No `Stop` button is present on Upcoming rows | visual-memory/admin/allocation/allocation-cancel-modal.png | upcomingCampaignId=`<seeded>` | P1 | Pending |
| TC_ALLOC_FUNC_023 | BRD §4 Rule 5 + §10.18 — Stop modal safe | FUNC | **SAFE variant**: Open Stop confirmation modal, assert title/body/buttons, then dismiss with Close — campaign stays Active. Validates modal contract without triggering the async Python stop | Admin; ≥1 Active campaign present (UAT id 289) | 1. Filter Status=`Active` 2. In Active row Actions cell click `button:has-text("Stop")` 3. Wait for `.ant-modal-content` 4. Read `.ant-modal-title`, `.ant-modal-body`, both buttons 5. Click `.ant-modal-content button:has-text("Close")` 6. Re-check row status | Modal `.ant-modal-content` opens with orange warning icon. Title `.ant-modal-title` = **"Stop Allocation Now?"**. Body `.ant-modal-body` = **"Campaign will move to Stopped."**. Dismiss button = `Close` (outline `ant-btn-default ant-btn-variant-outlined`). Confirm button = `Yes, Stop Now` (red `ant-btn-primary ant-btn-dangerous`). After clicking Close, modal closes; row status remains `Active` | visual-memory/admin/allocation/allocation-stop-modal.png | activeCampaignId=`289` | P1 | Pending |
| TC_ALLOC_FUNC_024 | BRD §4 Rule 5 + §5 (Active→Stopped) + §10.18 — Stop confirm `[MANUAL-ONLY]` | FUNC | **DESTRUCTIVE variant**: Confirm the Stop action; campaign moves to Stopped. Per BRD §10.18 status flip is async via Python — TC must poll/refresh, not expect synchronous change | Admin; **dedicated disposable Active campaign** created for this run (do NOT use shared seeded campaign); BRD §4 Rule 5 | 1. Filter Status=`Active` 2. Click `button:has-text("Stop")` on the disposable campaign row 3. In modal click `.ant-modal-content button:has-text("Yes, Stop Now")` (red danger) 4. Wait for `networkidle` (Python callback) 5. Click filter-bar Refresh; poll up to 30 s 6. Re-read campaign row Status column | Modal closes immediately on click; campaign Status transitions `Active → Stopped` after Python callback completes (NOT synchronous — must Refresh / poll per §10.18). Row's Actions cell now exposes `View` only (Stop control removed for non-Active rows). Mark as `[MANUAL-ONLY]` for first execution — automation requires a disposable-campaign factory + polling helper | visual-memory/admin/allocation/allocation-stop-modal.png · visual-memory/admin/allocation/screenshot-desktop.png | disposableActiveCampaignId=`<created-this-run>` | P1 | Pending |
| TC_ALLOC_FUNC_025 | BRD §4 Rule 5 + §10.18 — Cancel modal safe | FUNC | **SAFE variant**: Open Cancel confirmation modal on Upcoming row, assert title/body/buttons, dismiss with Close — campaign stays Upcoming | Admin; ≥1 Upcoming campaign present (e.g. "Test" STATIC Upcoming) | 1. Filter Status=`Upcoming` 2. In Upcoming row Actions cell click `button:has-text("Cancel")` 3. Wait for `.ant-modal-content` 4. Read `.ant-modal-title`, `.ant-modal-body`, both buttons 5. Click `.ant-modal-content button:has-text("Close")` 6. Re-check row status | Modal `.ant-modal-content` opens. Title `.ant-modal-title` = **"Cancel Allocation?"**. Body `.ant-modal-body` = **"This will cancel the upcoming campaign."**. Dismiss = `Close` (outline). Confirm = `Yes, Cancel` (red `ant-btn-dangerous`). After Close, modal closes; row status remains `Upcoming` | visual-memory/admin/allocation/allocation-cancel-modal.png | upcomingCampaignId=`<seeded>` | P1 | Pending |
| TC_ALLOC_FUNC_026 | BRD §4 Rule 5 + §5 (Upcoming→Cancelled) + §10.18 — Cancel confirm `[MANUAL-ONLY]` | FUNC | **DESTRUCTIVE variant**: Confirm Cancel; Upcoming campaign moves to Cancelled. Async via Python per §10.18 — poll, do not expect synchronous flip | Admin; **dedicated disposable Upcoming campaign** created for this run (e.g. STATIC, startTime=now+30min) | 1. Filter Status=`Upcoming` 2. Click `button:has-text("Cancel")` on disposable row 3. In modal click `.ant-modal-content button:has-text("Yes, Cancel")` (red danger) 4. Wait for `networkidle` 5. Refresh; poll up to 30 s 6. Re-read status | Modal closes; campaign Status transitions `Upcoming → Cancelled` after Python callback (NOT synchronous — must Refresh / poll per §10.18). Row's Actions cell now exposes `View` only. Mark `[MANUAL-ONLY]` first execution — needs disposable-campaign factory + polling helper | visual-memory/admin/allocation/allocation-cancel-modal.png · visual-memory/admin/allocation/screenshot-desktop.png | disposableUpcomingCampaignId=`<created-this-run>` | P1 | Pending |
| TC_ALLOC_NEG_027 | INDEX.md Stop modal naming note | NEG | Dismiss button label is exactly `Close` (NOT `Cancel`). Guard against TC steps written against the older "Cancel" assumption — selector would miss | Admin; Stop modal open (TC_ALLOC_FUNC_023 precondition) | 1. Open Stop modal 2. Try `.ant-modal-content button:has-text("Cancel")` 3. Try `.ant-modal-content button:has-text("Close")` | The first selector finds **zero** elements (no Cancel button). The second finds the dismiss button. Confirms the INDEX.md note: label is `Close` on the Stop modal | visual-memory/admin/allocation/allocation-stop-modal.png | activeCampaignId=`289` | P2 | Pending |
| TC_ALLOC_FUNC_028 | BRD §5 (auto-Completed) | FUNC | Active campaign auto-transitions to Completed at End Time | Active campaign with End Time = now+2min | 1. Wait until End Time elapses 2. Refresh list | Campaign row status updates to `Completed` | visual-memory/admin/allocation/screenshot-desktop.png | endTime=now+2min | P1 | Pending |
| TC_ALLOC_FUNC_029 | INDEX.md form-action Reset | FUNC | Reset clears all form fields without submitting | Admin on form with all fields populated | 1. Fill Project, Name, Type, Start, End, Description 2. Click `button.btn-book-outline` "Reset" | All fields cleared to initial state (Project placeholder, empty Name, Type=Static default, Start empty, End disabled, Description empty `0 / 255`) | visual-memory/admin/allocation/screenshot-desktop.png | N/A | P2 | Pending |
| TC_ALLOC_BIZ_030 | BRD §4 Rule 2 + §10.16 | BIZ | Single active campaign + 2-min pre-start blackout — second campaign create blocked when existing campaign is RUNNING or NOT_STARTED with startTime ≤ now+2min | Admin; one campaign already Active OR Upcoming with start ≤ 2 min away | 1. Attempt to create a second campaign with overlapping window | Submission rejected with backend error referencing active-campaign block; new row NOT added to table | visual-memory/admin/allocation/screenshot-desktop.png | second campaign overlap | P1 | Pending |
| TC_ALLOC_BIZ_031 | BRD §4 Rule 3 | BIZ | Tower prerequisite — ≥1 tower must be Active in Config CMS for campaign to be meaningful | Admin with project that has NO Active towers in Config CMS | 1. Create campaign with valid inputs for project lacking Active towers 2. Allow campaign to reach Active status | Buyer-side tower grid empty (downstream — out of Admin Allocation page scope; documents the BRD-level prerequisite). Admin form may allow creation but business outcome is meaningless | visual-memory/admin/allocation/screenshot-desktop.png | project with 0 Active towers | P2 | Pending |
| TC_ALLOC_BIZ_032 | BRD §10.17 (stale auto-FAILED) | BIZ | Stale past-window non-terminal campaigns auto-marked FAILED on new create — documents BRD §10.17 (engineering decision pending; current behaviour) | Admin with a leftover Upcoming campaign whose startTime is in the past | 1. Create a fresh campaign with valid inputs 2. After save, Refresh list | Old stale campaign row Status updates to `Failed` OR row disappears (per BRD §10 destroy-bug — `markAllocationCampaignFailed` destroys row in current source). Document actual observed behaviour in run notes | visual-memory/admin/allocation/screenshot-desktop.png | stale Upcoming row pre-existing | P2 | Pending |
| TC_ALLOC_NEG_033 | BRD §10.3 (XLSX binary error) | NEG | DYNAMIC/PHYSICAL_EVENT Excel validation failure returns XLSX binary per BRD §10.3 — UI must surface as downloaded error file, not JSON toast | Admin creating DYNAMIC or PHYSICAL_EVENT campaign | 1. Upload Excel with invalid rows (e.g. duplicate unit) 2. Click Save Campaign | HTTP 400 response with Content-Disposition attachment (`physical-event-allocation-errors.xlsx` or `dynamic-allocation-errors.xlsx`); admin sees download triggered | visual-memory/admin/allocation/allocation-form-validation-errors.png | invalid Excel | P1 | Pending |
| TC_ALLOC_NEG_034 | BRD §10.2 (PHYSICAL_EVENT requires commonPoolExcel) | NEG | PHYSICAL_EVENT without commonPoolExcel rejected with HTTP 400 per BRD §10.2 | Admin on form | 1. Select Allocation Type=Physical Event 2. Submit without uploading commonPoolExcel | Backend returns HTTP 400 "Common pool units Excel is required for PHYSICAL_EVENT allocation type"; campaign NOT created | visual-memory/admin/allocation/allocation-form-validation-errors.png | type=Physical Event; commonPoolExcel=missing | P1 | Pending |
| TC_ALLOC_NEG_035 | BRD §10.25 (DYNAMIC 20-cap) | NEG | DYNAMIC Excel rejects unit row when registrations ≥ 20 per BRD §10.25 hard cap | Admin creating DYNAMIC campaign | 1. Provide dynamicExcel with a unit having 20 prior registrations 2. Submit | HTTP 400 with error "Max registrations per unit exceeded (20)" delivered as XLSX per §10.3 | visual-memory/admin/allocation/allocation-form-validation-errors.png | dynamicExcel.unit_x.registrations=20 | P2 | Pending |
| TC_ALLOC_UI_036 | INDEX.md Campaign Detail Page — Physical Event | UI | Physical Event campaign detail page renders 6 KPI stat cards + 3 Campaign Actions buttons per INDEX.md detail-page table | Admin; a Completed Physical Event campaign exists (UAT id 288) | 1. From list, click `tbody.ant-table-tbody tr.ant-table-row a:has-text("View")` for the Physical Event row 2. Wait for navigation to `/admin/allocation/campaigns/<id>` 3. Inspect heading + KPI row + Campaign Actions card | URL = `/admin/allocation/campaigns/288`. `h2 "Physical Campaign Details"`, `h3` campaign name, status pill + `Physical Event` pill. KPI stat row shows 6 cards: Registrations Uploaded / Unique Assigned Units Uploaded / Common Pool Uploaded / Booked From Assigned Units (green) / Booked From Common Pool Units (teal) / Registrations Pending To Book (orange). `h4 "Campaign Actions"` section has 3 buttons: `Download Bookings` (primary green), `Download Pending` (outline), `Notify Registrants` (outline). Top-left `Back to Allocation Overview` button present | visual-memory/admin/allocation/allocation-export-ui.png | physicalEventCampaignId=`288` | P1 | Pending |
| TC_ALLOC_UI_037 | INDEX.md Campaign Detail Page — Static-Active | UI | Static-Active campaign detail page renders **only** 3 KPI cards and **only** `Download Bookings` action — NO Stop, NO Notify, NO Rounds. Stop is row-level on the list, not on detail | Admin; Active STATIC campaign exists (UAT id 289 `PE QA : Camp Test 4`) | 1. From list, click `View` on the STATIC Active row 2. Inspect heading + KPI row + Campaign Actions card | `h2 "Static Campaign Details"`, `h3 "PE QA : Camp Test 4"`. KPI row shows 3 cards: Initial Pending Registrations / Total Units Sold / Pending Registrations. `h4 "Campaign Actions"` shows **only** `Download Bookings`. NO `Stop`, `Cancel`, `Notify Registrants`, or `Rounds` UI on this page — Stop must be exercised from the list row | visual-memory/admin/allocation/screenshot-desktop.png | staticActiveCampaignId=`289` | P1 | Pending |
| TC_ALLOC_UI_038 | BRD §10.5 — Export UI; INDEX.md Export UI section | UI | Export UI on Admin = pair of buttons inside the Campaign Actions card (no separate "Export" button). STATIC detail shows only `Download Bookings`; Physical Event shows both Download buttons | Admin; Completed Physical Event campaign (id 288) AND a Stopped STATIC campaign (id 282) available | 1. Open detail of id 288 — inspect Campaign Actions buttons 2. Open detail of id 282 — inspect Campaign Actions buttons | id 288 (Physical Event, Completed): both `button:has-text("Download Bookings")` (class `btn-book-solid.ant-btn-primary`) and `button:has-text("Download Pending")` (class `btn-book-outline`) are present. id 282 (STATIC, Stopped): only `Download Bookings` is present, no `Download Pending`. Asymmetry matches INDEX.md table | visual-memory/admin/allocation/allocation-export-ui.png | physicalEventCampaignId=`288`; staticStoppedCampaignId=`282` | P2 | Pending |
| TC_ALLOC_FUNC_039 | BRD §10.5 — Download Bookings action | FUNC | Clicking `Download Bookings` on a Completed campaign streams an Excel/CSV file (BRD §10.5 — allotments export endpoint) | Admin on detail of a Completed campaign with allotments (UAT id 288) | 1. Click `button:has-text("Download Bookings")` 2. Observe browser download | Browser downloads an XLSX (or CSV) of booked allotments for that campaign. Filename and exact format from the live download — capture in execution notes | visual-memory/admin/allocation/allocation-export-ui.png | completedCampaignId=`288` | P2 | Pending |
| TC_ALLOC_FUNC_040 | BRD §10.5 (pending export — UI variant) | FUNC | Clicking `Download Pending` on Physical Event detail downloads the unbooked/pending allotments file | Admin on Physical Event Completed campaign detail (id 288) | 1. Click `button:has-text("Download Pending")` 2. Observe download | Browser downloads XLSX/CSV of pending allotments. Button is `btn-book-outline` style with download icon | visual-memory/admin/allocation/allocation-export-ui.png | physicalEventCampaignId=`288` | P3 | Pending |
| TC_ALLOC_UI_041 | BRD §10.6 — Notify modal | UI | Notify Registrants confirmation modal renders correct title/body/buttons (BRD §10.6 — Notify UI gated by Physical Event allocation type) | Admin on Physical Event campaign detail (id 288) | 1. Click `button:has-text("Notify Registrants")` 2. Wait for `.ant-modal-content` 3. Read modal contents 4. Click `Cancel` to dismiss | Modal opens with orange warning icon. Title `.ant-modal-title` = **"Notify Registrants?"**. Body = "This will generate unique QR codes and notify all registrants via SMS/WhatsApp." Buttons: `Cancel` (outline) + `Yes, Notify All` (primary blue). STATIC campaign detail does NOT expose Notify Registrants — verify by checking id 289 detail has no Notify button | visual-memory/admin/allocation/allocation-notify-ui.png | physicalEventCampaignId=`288` | P1 | Pending |
| TC_ALLOC_FUNC_042 | BRD §10.6 — Notify confirm `[MANUAL-ONLY]` | FUNC | **DESTRUCTIVE variant**: Confirm Notify Registrants triggers Kaleyra SMS/WhatsApp to all registrants of a Physical Event campaign | Admin on Physical Event Completed campaign detail with registrants; only run on disposable test campaign | 1. Click `Notify Registrants` 2. In modal click `button:has-text("Yes, Notify All")` 3. Observe UI confirmation (toast / state change) | Backend triggers notification queue (POST `/api/v1/admin/allocation/campaigns/:campaignId/notify` per BRD §10.6); UI confirms send queued. Real SMS/WhatsApp delivery via Kaleyra is downstream — out of in-page scope. Mark `[MANUAL-ONLY]` because action is irreversible and contacts real test registrants | visual-memory/admin/allocation/allocation-notify-ui.png | physicalEventCampaignId=`<disposable>` | P2 | Pending |
| TC_ALLOC_UI_043 | BRD §3 + §10.4 — Rounds UI exists on DYNAMIC detail | UI | DYNAMIC campaign detail page MUST show "Round-Wise Data" section per BRD §3 (Dynamic = round-based) | Admin; Active DYNAMIC campaign exists (UAT id 291 "Test dynamic campaign") | 1. From list, click `View` on the DYNAMIC row 2. Wait for navigation to `/admin/allocation/campaigns/291` 3. Inspect headings + section list | URL = `/admin/allocation/campaigns/291`. `h2 "Dynamic Campaign Details"`, `h3 "Test dynamic campaign"`. Page body contains the **"Round-Wise Data"** section heading. (Per BRD §10.4 the underlying endpoint is `GET /api/v1/admin/allocation/campaign/:campaignId/rounds` paginated page=1, limit=20 — verify in run notes) | visual-memory/admin/allocation/allocation-rounds-view.png | dynamicCampaignId=`291` | P1 | Pending |
| TC_ALLOC_NEG_044 | BRD §3 + §10.4 — Rounds NOT shown on non-DYNAMIC | NEG | STATIC and PHYSICAL_EVENT campaign detail pages MUST NOT render the "Round-Wise Data" section — Rounds UI is DYNAMIC-exclusive per BRD §3 | Admin; a STATIC campaign (id 289) AND a PHYSICAL_EVENT campaign (id 288) detail pages accessible | 1. Open STATIC campaign detail (id 289) — search the page for `"Round-Wise Data"` 2. Open PHYSICAL_EVENT campaign detail (id 288) — search the page for `"Round-Wise Data"` | Neither page contains the "Round-Wise Data" heading. STATIC detail shows 3 KPI cards + Download Bookings only. PHYSICAL_EVENT detail shows 6 KPI cards + Download Bookings/Download Pending/Notify Registrants only. Rounds is exclusive to DYNAMIC | visual-memory/admin/allocation/allocation-rounds-view.png · visual-memory/admin/allocation/allocation-export-ui.png | staticCampaignId=`289`; physicalEventCampaignId=`288` | P1 | Pending |
| TC_ALLOC_EDGE_045 | BRD §10.1 (default project fallback) | EDGE | Backend silently substitutes projectId=2 on UAT when client omits projectId per BRD §10.1 (API-level — no UI surface) | API-direct call (or form intercepted to strip projectId) | 1. Submit campaign-create payload with `projectId` field missing 2. Inspect response | API returns 200 with campaign created against projectId=2 (UAT). UI still enforces required Project marker. Documents observed backend behaviour, not desired | [NO-VISUAL-EVIDENCE] — API-level only | projectId omitted | P2 | Pending |
| TC_ALLOC_EDGE_046 | BRD §10.26 (PHYSICAL_EVENT asymmetry) | EDGE | PHYSICAL_EVENT does NOT enforce typology-match or pool/assigned overlap — STATIC does. Documents asymmetry; backend-only | Admin creating PHYSICAL_EVENT with mismatched typology | 1. Provide commonPoolExcel with typology mismatching project 2. Submit | Submission accepted (no enforcement). Same payload as STATIC would be rejected. Flagged to Dev Agent in BRD §10 | [NO-VISUAL-EVIDENCE] — backend asymmetry | typologyMismatch in commonPoolExcel | P3 | Pending |
| TC_ALLOC_EDGE_047 | BRD §10.19 (20-min hold release) | EDGE | Unit HOLD auto-releases to AVAILABLE 20 minutes after the hold timestamp — observable downstream in Towers / Buyer portal | Buyer-side unit in HOLD ≥ 20 min without payment | 1. Wait 20+ min after hold timestamp 2. Refresh Towers module (NOT Allocation page) | Unit status flips HOLD → AVAILABLE. Cross-module observation — Allocation INDEX.md does not capture this UI; Towers module INDEX.md owns the evidence | [NO-VISUAL-EVIDENCE] — cross-module (Towers) | holdTimestamp=now-21min | P2 | Pending |
| TC_ALLOC_EDGE_048 | BRD §10.20 (payment-status release) | EDGE | Payment statuses `cancelled`/`bounced`/`failed` immediately release the unit (no 20-min wait) | Buyer attempted payment → one of the three statuses fired | 1. Trigger one of the three payment statuses 2. Admin refreshes Towers grid | Unit returns to AVAILABLE immediately. Cross-module observation — owned by Towers / Buyer INDEX.md | [NO-VISUAL-EVIDENCE] — cross-module (Towers/Buyer) | paymentStatus in ['cancelled','bounced','failed'] | P2 | Pending |

---

## Sheet 2 — Automation Candidates

TCs with FULL visual evidence AND no destructive-state dependency. `[MANUAL-ONLY]` destructive TCs (FUNC_024, FUNC_026, FUNC_042) and API-only/cross-module edge TCs (EDGE_045–048) are excluded.

| TC_ID | Module | Type | Automatable | Complexity | Playwright Suite | Visual Evidence Status | Notes |
|-------|--------|------|-------------|------------|------------------|------------------------|-------|
| TC_ALLOC_UI_001 | Allocation | UI | Yes | Low | ui-ux | FULL | Page render check |
| TC_ALLOC_UI_002 | Allocation | UI | Yes | Low | ui-ux | FULL | Static UI element assertions; uses screenshot-desktop + screenshot-ui |
| TC_ALLOC_UI_003 | Allocation | UI | Yes | Low | ui-ux | FULL | Filter bar disabled-state check |
| TC_ALLOC_UI_004 | Allocation | UI | Yes | Low | ui-ux | FULL | Sidebar nav assertion |
| TC_ALLOC_UI_005 | Allocation | UI | Yes | Low | ui-ux | FULL | Status filter enum — asserts no "Approved" option |
| TC_ALLOC_FUNC_006 | Allocation | FUNC | Yes | Medium | e2e | FULL | STATIC happy path — needs data factory for project + dates |
| TC_ALLOC_FUNC_007 | Allocation | FUNC | Yes | Medium | e2e | FULL | DYNAMIC happy path |
| TC_ALLOC_FUNC_008 | Allocation | FUNC | Partial | High | e2e | FULL | PHYSICAL_EVENT needs file-upload fixture |
| TC_ALLOC_VAL_009 | Allocation | VAL | Yes | Low | regression | FULL | 4 inline errors — assert exact strings |
| TC_ALLOC_VAL_010 | Allocation | VAL | Yes | Low | regression | FULL | 3-min lead validation |
| TC_ALLOC_VAL_011 | Allocation | VAL | Yes | Low | regression | FULL | Required Campaign Name |
| TC_ALLOC_VAL_012 | Allocation | VAL | Yes | Low | regression | FULL | Required Project (UI level) |
| TC_ALLOC_VAL_013 | Allocation | VAL | Yes | Low | ui-ux | FULL | End Time disabled gating |
| TC_ALLOC_VAL_014 | Allocation | VAL | Yes | Low | regression | FULL | End < Start chronology |
| TC_ALLOC_VAL_015 | Allocation | VAL | Yes | Low | ui-ux | FULL | Description 255-char limit |
| TC_ALLOC_FUNC_016 | Allocation | FUNC | Yes | Low | ui-ux | FULL | Filter bar gated controls |
| TC_ALLOC_FUNC_017 | Allocation | FUNC | Yes | Medium | e2e | FULL | Search filter — needs seeded campaign |
| TC_ALLOC_FUNC_018 | Allocation | FUNC | Yes | Low | regression | FULL | Empty-search state — visual matches allocation-empty-state.png |
| TC_ALLOC_FUNC_019 | Allocation | FUNC | Yes | Low | e2e | FULL | Refresh re-pull |
| TC_ALLOC_FUNC_020 | Allocation | FUNC | Partial | High | e2e | FULL | Time-based transition — use seeded short-window campaign |
| TC_ALLOC_UI_021 | Allocation | UI | Yes | Low | ui-ux | FULL | Active row Actions — View + Stop only |
| TC_ALLOC_UI_022 | Allocation | UI | Yes | Low | ui-ux | FULL | Upcoming row Actions — View + Cancel only |
| TC_ALLOC_FUNC_023 | Allocation | FUNC | Yes | Low | e2e | FULL | **Safe** Stop modal open + assert + Close — non-destructive |
| TC_ALLOC_FUNC_025 | Allocation | FUNC | Yes | Low | e2e | FULL | **Safe** Cancel modal open + assert + Close — non-destructive |
| TC_ALLOC_NEG_027 | Allocation | NEG | Yes | Low | regression | FULL | Selector-naming guard (Close, not Cancel) |
| TC_ALLOC_FUNC_028 | Allocation | FUNC | Partial | High | e2e | FULL | Auto-complete time-based |
| TC_ALLOC_FUNC_029 | Allocation | FUNC | Yes | Low | ui-ux | FULL | Reset clears form |
| TC_ALLOC_BIZ_030 | Allocation | BIZ | Yes | Medium | regression | FULL | Single-active + 2-min blackout |
| TC_ALLOC_BIZ_031 | Allocation | BIZ | Partial | Medium | regression | FULL | Config CMS precondition (no Active towers) |
| TC_ALLOC_BIZ_032 | Allocation | BIZ | Partial | Medium | regression | FULL | Stale-leftover seed needed |
| TC_ALLOC_NEG_033 | Allocation | NEG | Yes | Medium | regression | FULL | XLSX binary error — assert content-disposition |
| TC_ALLOC_NEG_034 | Allocation | NEG | Yes | Low | regression | FULL | Missing commonPoolExcel |
| TC_ALLOC_NEG_035 | Allocation | NEG | Yes | Medium | regression | FULL | DYNAMIC 20-cap |
| TC_ALLOC_UI_036 | Allocation | UI | Yes | Low | ui-ux | FULL | Physical Event detail layout (6 KPI + 3 buttons) |
| TC_ALLOC_UI_037 | Allocation | UI | Yes | Low | ui-ux | FULL | Static-Active detail (3 KPI + Download Bookings only) |
| TC_ALLOC_UI_038 | Allocation | UI | Yes | Low | ui-ux | FULL | Export UI asymmetry (Static vs Physical Event) |
| TC_ALLOC_FUNC_039 | Allocation | FUNC | Partial | Medium | e2e | FULL | Download Bookings — assert download triggered |
| TC_ALLOC_FUNC_040 | Allocation | FUNC | Partial | Medium | e2e | FULL | Download Pending — assert download triggered |
| TC_ALLOC_UI_041 | Allocation | UI | Yes | Low | e2e | FULL | Notify modal open + assert + Cancel — non-destructive |
| TC_ALLOC_UI_043 | Allocation | UI | Yes | Low | ui-ux | FULL | DYNAMIC detail Round-Wise Data section present |
| TC_ALLOC_NEG_044 | Allocation | NEG | Yes | Low | ui-ux | FULL | STATIC + PHYSICAL_EVENT details have NO Round-Wise Data |

**Excluded from Sheet 2 (intentional):**
- `TC_ALLOC_FUNC_024` — `[MANUAL-ONLY]` destructive Stop confirm; needs disposable Active campaign + async-polling helper
- `TC_ALLOC_FUNC_026` — `[MANUAL-ONLY]` destructive Cancel confirm; needs disposable Upcoming campaign + async-polling helper
- `TC_ALLOC_FUNC_042` — `[MANUAL-ONLY]` destructive Notify confirm; triggers real Kaleyra send
- `TC_ALLOC_EDGE_045` — API-level only (no UI surface)
- `TC_ALLOC_EDGE_046` — backend asymmetry (no UI surface)
- `TC_ALLOC_EDGE_047` — cross-module (Towers INDEX.md owns evidence)
- `TC_ALLOC_EDGE_048` — cross-module (Towers / Buyer INDEX.md owns evidence)

---

## Sheet 3 — Bug Template

| Bug ID | TC_ID | Severity | Steps to Reproduce | Actual | Expected | Environment | Status |
|--------|-------|----------|--------------------|--------|----------|-------------|--------|
| BUG_NNN | TC_ALLOC_XXX_NNN | S1/S2/S3/S4 | (numbered) | | | UAT | New/Triaged/Fixed/Closed |

---

## Visual Evidence Coverage (per-screen)

| INDEX.md screen | Referenced by TC(s) |
|----------------|---------------------|
| `screenshot-desktop.png` | UI_001, UI_002, UI_003, UI_004, UI_005, FUNC_006, FUNC_007, FUNC_008, VAL_013, FUNC_016, FUNC_017, FUNC_019, FUNC_020, UI_021, FUNC_028, FUNC_029, BIZ_030, BIZ_031, BIZ_032, UI_037, FUNC_024 |
| `screenshot-ui.png` | UI_002 |
| `allocation-form-validation-errors.png` | VAL_009, VAL_010, VAL_011, VAL_012, VAL_014, NEG_033, NEG_034, NEG_035 |
| `allocation-empty-state.png` | FUNC_018 |
| `allocation-export-ui.png` | UI_036, UI_038, FUNC_039, FUNC_040, NEG_044 |
| `allocation-notify-ui.png` | UI_041, FUNC_042 |
| `allocation-stop-modal.png` | UI_021, FUNC_023, FUNC_024, NEG_027 |
| `allocation-rounds-view.png` | UI_043, NEG_044 |
| `allocation-cancel-modal.png` | UI_022, FUNC_025, FUNC_026 |

All 9 captured screens are cited by ≥1 TC. No orphan screens.
